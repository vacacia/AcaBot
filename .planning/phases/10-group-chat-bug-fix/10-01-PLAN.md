---
phase: 10
slug: group-chat-bug-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime_config/sessions/qq/group/1097619430/session.yaml
  - runtime_config/sessions/qq/group/1039173249/session.yaml
  - runtime_config/sessions/qq/group/742824007/session.yaml
  - tests/runtime/test_session_runtime.py
autonomous: false
requirements:
  - GROUP-01
must_haves:
  truths:
    - "Bot 在 group scene 对普通消息不回复（silent_drop 生效）"
    - "Bot 在 group scene 对 @ 消息回复（respond 生效）"
    - "Bot 在 group scene 对回复机器人消息回复（respond 生效）"
    - "pytest 回归测试覆盖 surface 命名约定"
  artifacts:
    - path: "runtime_config/sessions/qq/group/1097619430/session.yaml"
      provides: "测试群 session 配置，surface 键正确命名"
      contains: "message.plain:"
    - path: "runtime_config/sessions/qq/group/1039173249/session.yaml"
      provides: "feizhu 群 session 配置，surface 键正确命名"
      contains: "message.plain:"
    - path: "runtime_config/sessions/qq/group/742824007/session.yaml"
      provides: "aozi 群 session 配置，surface 键正确命名"
      contains: "message.plain:"
    - path: "tests/runtime/test_session_runtime.py"
      provides: "pytest 回归测试"
      contains: "test_surface_naming_resolves_admission_correctly"
  key_links:
    - from: "session.yaml surface keys"
      to: "session_runtime._surface_candidates()"
      via: "resolve_surface() matching"
      pattern: "message\\.plain.*in.*surfaces"
    - from: "resolve_surface()"
      to: "resolve_admission()"
      via: "SurfaceResolution.domain"
      pattern: "domain.*resolve_admission"
---

<objective>
修复群聊消息响应行为——让 bot 根据 session config 的 admission domain 配置，对群聊消息做出正确的响应（仅对 @ 机器人或回复机器人的消息回复，对普通群消息 silent_drop）。

Purpose: 修复 surface 命名不一致导致的 bug（Config 写了 `message`/`message_mention`/`message_reply`，代码期望 `message.plain`/`message.mention`/`message.reply_to_bot`），使 `resolve_surface()` 能正确匹配 surface 并返回对应的 admission mode。
Output: 修复后的 session.yaml 文件 + pytest 回归测试 + E2E 验证
</objective>

<context>
@runtime_config/sessions/qq/group/1097619430/session.yaml
@runtime_config/sessions/qq/group/1039173249/session.yaml
@runtime_config/sessions/qq/group/742824007/session.yaml
@tests/runtime/test_session_runtime.py
@src/acabot/runtime/control/session_runtime.py (L427-454, L141-155, L206-220)

# Surface 命名约定 (session_runtime.py 确认)
# 代码生成的候选链:
#   普通消息 → ["message.plain"]
#   @ 消息   → ["message.mention", "message.plain"]
#   回复消息 → ["message.reply_to_bot", "message.plain"]
#
# 当前 Config 错误命名 vs 正确命名:
#   message        → message.plain
#   message_mention → message.mention
#   message_reply   → message.reply_to_bot
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复三个群组的 session.yaml surface 命名</name>
  <files>runtime_config/sessions/qq/group/1097619430/session.yaml,runtime_config/sessions/qq/group/1039173249/session.yaml,runtime_config/sessions/qq/group/742824007/session.yaml</files>
  <read_first>
runtime_config/sessions/qq/group/1097619430/session.yaml
runtime_config/sessions/qq/group/1039173249/session.yaml
runtime_config/sessions/qq/group/742824007/session.yaml
src/acabot/runtime/control/session_runtime.py (L427-454)
  </read_first>
  <action>
在每个 session.yaml 的 `surfaces:` 块下，将三个 surface 键重命名：

1. `message:` → `message.plain:`
2. `message_mention:` → `message.mention:`
3. `message_reply:` → `message.reply_to_bot:`

注意：只改键名，缩进和子结构保持不变。

三个文件各自的 admission mode 如下（不要改变这些值，只改键名）:

**1097619430 (test group):**
- message.plain → silent_drop
- message.mention → respond
- message.reply_to_bot → respond

**1039173249 (feizhu):**
- message.plain → record_only
- message.mention → record_only
- message.reply_to_bot → record_only

**742824007 (aozi):**
- message.plain → record_only
- message.mention → respond
- message.reply_to_bot → record_only
  </action>
  <verify>
<automated>
cd /home/acacia/AcaBot && grep -E "^\s+message\.plain:|^\s+message\.mention:|^\s+message\.reply_to_bot:" runtime_config/sessions/qq/group/1097619430/session.yaml runtime_config/sessions/qq/group/1039173249/session.yaml runtime_config/sessions/qq/group/742824007/session.yaml && grep -E "^\s+message:|^\s+message_mention:|^\s+message_reply:" runtime_config/sessions/qq/group/1097619430/session.yaml runtime_config/sessions/qq/group/1039173249/session.yaml runtime_config/sessions/qq/group/742824007/session.yaml | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: no old surface names remain"</automated>
  </verify>
  <done>
三个 session.yaml 文件中的 surface 键已重命名为 `message.plain`、`message.mention`、`message.reply_to_bot`，旧的 `message`、`message_mention`、`message_reply` 键不再存在。
</done>
</task>

<task type="auto">
  <name>Task 2a: 编写 pytest 回归测试 test_surface_naming_resolves_admission_correctly</name>
  <files>tests/runtime/test_session_runtime.py</files>
  <read_first>
tests/runtime/test_session_runtime.py
src/acabot/runtime/control/session_runtime.py (L427-454, L141-155, L206-220)
  </read_first>
  <action>
在 `tests/runtime/test_session_runtime.py` 中添加一个新的测试函数 `test_surface_naming_resolves_admission_correctly`。

此测试验证：当 surface 键使用正确命名 `message.plain`/`message.mention`/`message.reply_to_bot` 时，`resolve_surface()` 能正确匹配，`resolve_admission()` 返回正确的 mode。

测试逻辑：
1. 写入一个临时 session config，使用正确的 surface 键名（`message.plain` 配置 silent_drop，`message.mention` 配置 respond）
2. 构造两条测试事件：plain 消息（`mentions_self=False, reply_targets_self=False`）和 mention 消息（`mentions_self=True`）
3. 调用 `SessionRuntime` 处理每条事件
4. 验证 plain 消息返回 `admission.mode == "silent_drop"`，mention 消息返回 `admission.mode == "respond"`

参考现有测试 `_write_session()` 辅助函数的写法。注意：这个测试是为了确保正确命名能正常工作，不测试错误命名（那是 bug，不是回归测试的目标）。
  </action>
  <verify>
<automated>cd /home/acacia/AcaBot && grep -n "def test_surface_naming_resolves_admission_correctly" tests/runtime/test_session_runtime.py && echo "PASS: test function exists"</automated>
  </verify>
  <done>
pytest 测试函数 `test_surface_naming_resolves_admission_correctly` 已写入 `tests/runtime/test_session_runtime.py`。
</done>
</task>

<task type="auto">
  <name>Task 2b: 运行回归测试验证 admission 逻辑</name>
  <files>tests/runtime/test_session_runtime.py</files>
  <action>
运行刚刚创建的 `test_surface_naming_resolves_admission_correctly` 测试，验证正确 surface 命名下 admission 逻辑正确工作。
  </action>
  <verify>
<automated>cd /home/acacia/AcaBot && pytest tests/runtime/test_session_runtime.py::test_surface_naming_resolves_admission_correctly -x -q</automated>
  </verify>
  <done>
pytest 测试 `test_surface_naming_resolves_admission_correctly` 通过，验证正确 surface 命名下 admission 逻辑正确工作。
</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: E2E 验证——普通消息不回复、@ 消息回复、回复消息回复</name>
  <what-built>Phase 10 config fix + unit test</what-built>
  <verify>
<automated>echo 'Manual E2E verification required - see how-to-verify below'</automated>
  </verify>
  <how-to-verify>
**前提：** AcaBot runtime 正在运行，WebSocket 连接到测试群 1097619430（token: 80iV<RBrHtQdmp0r, bot ID: 3482263824）

**步骤 1 — 普通消息（应无回复）：**
```bash
python3 scripts/send_test_event.py --group-id 1097619430 --text "普通消息" --message-type group --wait-seconds 5
```
观察：bot 应完全不回复。

**步骤 2 — @ 消息（应有回复）：**
```bash
python3 scripts/send_test_event.py --group-id 1097619430 --text "hello" --message-type group --mention-self --wait-seconds 8
```
观察：bot 应回复消息。

**步骤 3 — 回复机器人消息（应有回复）：**
```bash
python3 scripts/send_test_event.py --group-id 1097619430 --text "replying" --message-type group --reply-id <msg_id> --wait-seconds 8
```
观察：bot 应回复消息。

（msg_id 从步骤 1 或 2 的机器人回复中获取）

**成功标准：**
- 步骤 1：无任何回复
- 步骤 2：bot 发送了回复
- 步骤 3：bot 发送了回复
  </how-to-verify>
  <resume-signal>
输入 "approved" 表示 E2E 验证通过，或描述问题（哪个步骤失败、实际观察到什么）
</resume-signal>
</task>

</tasks>

<verification>
## Phase 10 验证检查清单

| 步骤 | 命令 | 预期结果 |
|------|------|----------|
| YAML 键重命名验证 | `grep -E "message\.plain\|message\.mention\|message\.reply_to_bot" <3个文件>` | 找到 9 处正确键名 |
| 旧键名不存在 | `grep -E "^  message:\|^  message_mention:\|^  message_reply:" <3个文件>` | 无匹配 |
| Unit test (函数存在) | `grep "def test_surface_naming_resolves_admission_correctly"` | 找到函数定义 |
| Unit test (运行) | `pytest tests/runtime/test_session_runtime.py::test_surface_naming_resolves_admission_correctly -x` | PASSED |
| E2E plain 消息 | `python3 scripts/send_test_event.py --group-id 1097619430 --text "普通" --message-type group` | 无回复 |
| E2E @ 消息 | `python3 scripts/send_test_event.py --group-id 1097619430 --text "hello" --message-type group --mention-self` | 有回复 |
| E2E 回复消息 | `python3 scripts/send_test_event.py --group-id 1097619430 --text "replying" --message-type group --reply-id <id>` | 有回复 |
</verification>

<success_criteria>
## Success Criteria — GROUP-01

1. **YAML 重命名完成：** 三个群组 session.yaml 的 surface 键从 `message`/`message_mention`/`message_reply` 改为 `message.plain`/`message.mention`/`message.reply_to_bot`
2. **Unit test 存在：** `grep "def test_surface_naming_resolves_admission_correctly"` 找到函数定义
3. **Unit test 通过：** `pytest tests/runtime/test_session_runtime.py::test_surface_naming_resolves_admission_correctly -x` PASSED
4. **E2E 普通消息：** 发送普通群消息，bot 不回复（silent_drop 生效）
5. **E2E @ 消息：** 发送 @ 机器人的消息，bot 回复（respond 生效）
6. **E2E 回复消息：** 发送回复机器人的消息，bot 回复（respond 生效）
</success_criteria>

<output>
After completion, create `.planning/phases/10-group-chat-bug-fix/10-01-SUMMARY.md`
</output>
