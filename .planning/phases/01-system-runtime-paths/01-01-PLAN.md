---
phase: 01-system-runtime-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/acabot/runtime/control/config_control_plane.py
  - src/acabot/runtime/control/control_plane.py
  - src/acabot/runtime/control/http_api.py
  - tests/runtime/test_webui_api.py
autonomous: true
requirements:
  - SYS-02
  - SYS-03
  - OPS-02
user_setup: []
must_haves:
  truths:
    - `GET /api/system/configuration` 返回单一的后端权威快照，覆盖 gateway、filesystem、admins 和解析后的路径总览。
    - gateway / filesystem / admins 的写入结果必须暴露明确的应用状态语义，而不是模糊的“已保存”。
    - 解析后的路径数据必须由后端基于正式配置真源计算，不能由 Vue 侧重算。
  artifacts:
    - `src/acabot/runtime/control/config_control_plane.py` 提供路径总览 helper 和应用结果整形。
    - `src/acabot/runtime/control/control_plane.py` 暴露系统级聚合视图。
    - `src/acabot/runtime/control/http_api.py` 暴露 `/api/system/configuration`。
    - `tests/runtime/test_webui_api.py` 校验新的系统快照和结果语义。
  key_links:
    - `/api/system/configuration` -> `RuntimeControlPlane.get_system_configuration_view()` -> `RuntimeConfigControlPlane` 路径 helper
    - gateway / filesystem / admins 的 PUT 流程返回显式 apply status，供系统页直接消费
---

<objective>
为 Phase 1 建立系统配置的后端契约。

目的：给系统页提供唯一的后端权威视图，统一共享配置、解析后路径和保存/应用语义。
产出：统一的系统配置 endpoint、显式 apply-result payload，以及对应的后端契约测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-runtime-paths/01-CONTEXT.md
@.planning/phases/01-system-runtime-paths/01-RESEARCH.md
@.planning/phases/01-system-runtime-paths/01-UI-SPEC.md
@docs/08-webui-and-control-plane.md
@docs/09-config-and-runtime-files.md
@src/acabot/runtime/control/http_api.py
@src/acabot/runtime/control/control_plane.py
@src/acabot/runtime/control/config_control_plane.py
@src/acabot/runtime/bootstrap/config.py
@src/acabot/runtime/bootstrap/builders.py
@src/acabot/runtime/bootstrap/__init__.py
@tests/runtime/test_webui_api.py
</context>

<tasks>

<task type="auto">
  <name>任务 1：补齐后端权威的运行时路径总览 helper</name>
  <files>src/acabot/runtime/control/config_control_plane.py</files>
  <read_first>src/acabot/runtime/control/config_control_plane.py, src/acabot/runtime/bootstrap/config.py, src/acabot/runtime/bootstrap/builders.py, src/acabot/runtime/bootstrap/__init__.py, docs/09-config-and-runtime-files.md</read_first>
  <action>在 `RuntimeConfigControlPlane` 上新增 `get_runtime_path_overview()`，返回当前系统真源对应的绝对路径字符串视图。至少包含这些 key：`config_path`、`storage_mode`、`filesystem_base_dir`、`profiles_dir`、`prompts_dir`、`sessions_dir`、`computer_root_dir`、`sticky_notes_dir`、`long_term_memory_storage_dir`、`resolved_skill_catalog_dirs`、`resolved_subagent_catalog_dirs`，以及在可用时返回 `backend_session_path`。这些值必须建立在现有 helper 之上，例如 `_filesystem_conf()`、`_resolved_catalog_dir_views()`、`_resolve_filesystem_path(...)`、`resolve_runtime_path(...)`，以及 bootstrap 已在使用的 repo 默认目录（`profiles`、`prompts`、`sessions`、`sticky-notes`、`long-term-memory/lancedb`、`Path.home() / ".acabot" / "workspaces"`）。不要引入第二套路径解析规则。</action>
  <verify>rg -n "def get_runtime_path_overview|filesystem_base_dir|sticky_notes_dir|long_term_memory_storage_dir|backend_session_path" src/acabot/runtime/control/config_control_plane.py</verify>
  <acceptance_criteria>
    - `src/acabot/runtime/control/config_control_plane.py` 包含 `def get_runtime_path_overview`
    - `src/acabot/runtime/control/config_control_plane.py` 包含精确 key：`filesystem_base_dir`、`profiles_dir`、`prompts_dir`、`sessions_dir`
    - `src/acabot/runtime/control/config_control_plane.py` 包含精确 key：`computer_root_dir`、`sticky_notes_dir`、`long_term_memory_storage_dir`
  </acceptance_criteria>
  <done>后端路径总览 helper 已存在，并能从后端真源暴露必需的解析后路径 key。</done>
</task>

<task type="auto">
  <name>任务 2：增加统一的系统配置快照和显式 apply-result 语义</name>
  <files>src/acabot/runtime/control/config_control_plane.py, src/acabot/runtime/control/control_plane.py, src/acabot/runtime/control/http_api.py</files>
  <read_first>src/acabot/runtime/control/config_control_plane.py, src/acabot/runtime/control/control_plane.py, src/acabot/runtime/control/http_api.py, .planning/phases/01-system-runtime-paths/01-CONTEXT.md, .planning/phases/01-system-runtime-paths/01-UI-SPEC.md</read_first>
  <action>实现 `RuntimeControlPlane.get_system_configuration_view()`，并通过 `GET /api/system/configuration` 暴露出来。返回 payload 必须聚合 `gateway`、`filesystem`、`admins` 和 `paths`，并补上系统页需要的最小 meta 字段。更新保存流程的返回结果，使其显式区分写入状态和应用状态。目标形状是：filesystem 和 admins 保存成功时返回 `apply_status: "applied"`、`restart_required: false` 和一条人话 `message`；gateway 保存成功时返回 `apply_status: "restart_required"`、`restart_required: true`，并返回文案 `已保存，需要重启后生效`。对于 filesystem 这类“写入后重载”的流程，文件保存成功但 reload 失败时，要返回 `apply_status: "apply_failed"` 和技术细节字段，不能静默宣称成功。保持 `http_api.py` 为薄适配层，聚合和结果整形逻辑应留在 control/config 层。</action>
  <verify>rg -n "/api/system/configuration|def get_system_configuration_view|apply_status|restart_required|已保存，需要重启后生效|apply_failed" src/acabot/runtime/control/http_api.py src/acabot/runtime/control/control_plane.py src/acabot/runtime/control/config_control_plane.py</verify>
  <acceptance_criteria>
    - `src/acabot/runtime/control/http_api.py` 包含精确字符串 `["system", "configuration"]`
    - `src/acabot/runtime/control/control_plane.py` 包含 `def get_system_configuration_view`
    - `src/acabot/runtime/control/config_control_plane.py` 包含精确字符串 `apply_status`
    - `src/acabot/runtime/control/config_control_plane.py` 包含精确字符串 `restart_required`
    - `src/acabot/runtime/control/config_control_plane.py` 包含精确文案 `已保存，需要重启后生效`
  </acceptance_criteria>
  <done>系统页已经有唯一的 GET 快照入口，各分区保存流程也已暴露显式 apply-result 语义。</done>
</task>

<task type="auto">
  <name>任务 3：扩展系统配置语义的后端契约测试</name>
  <files>tests/runtime/test_webui_api.py</files>
  <read_first>tests/runtime/test_webui_api.py, src/acabot/runtime/control/http_api.py, src/acabot/runtime/control/control_plane.py, src/acabot/runtime/control/config_control_plane.py</read_first>
  <action>在 `tests/runtime/test_webui_api.py` 中新增有针对性的测试，验证以下行为：`GET /api/system/configuration` 返回 `gateway`、`filesystem`、`admins` 和 `paths`；解析后的路径字段是绝对路径字符串；gateway 保存时返回 `apply_status == "restart_required"`；filesystem 保存成功时返回 `apply_status == "applied"`；admins 保存成功时返回 `apply_status == "applied"`。复用现有 fake server / request helper，不要引入新的测试 harness。</action>
  <verify>PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "system_configuration or filesystem or admin_actor_ids or gateway"</verify>
  <acceptance_criteria>
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `"/api/system/configuration"`
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `apply_status`
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `restart_required`
    - `PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "system_configuration or filesystem or admin_actor_ids or gateway"` 退出码为 0
  </acceptance_criteria>
  <done>后端系统配置契约已有可执行测试覆盖。</done>
</task>

</tasks>

<verification>
在声明计划完成之前：
- [ ] `PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "system_configuration or filesystem or admin_actor_ids or gateway"` 通过
- [ ] `rg -n "/api/system/configuration|apply_status|restart_required" src/acabot/runtime/control/http_api.py src/acabot/runtime/control/control_plane.py src/acabot/runtime/control/config_control_plane.py` 能看到新增契约点
- [ ] 本计划没有把路径解析逻辑复制到 Vue 文件里
</verification>

<success_criteria>

- 所有任务完成
- 所有验证检查通过
- 没有引入新的错误或告警
- 系统配置快照由后端权威提供
- 保存返回值明确区分 applied、restart-required 和 apply-failed
  </success_criteria>

<output>
完成后创建 `.planning/phases/01-system-runtime-paths/01-01-SUMMARY.md`
</output>
