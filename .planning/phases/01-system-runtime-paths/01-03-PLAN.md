---
phase: 01-system-runtime-paths
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - webui/src/views/SystemView.vue
  - webui/src/router.ts
  - webui/src/components/AppSidebar.vue
  - webui/src/lib/api.ts
  - tests/runtime/test_webui_api.py
autonomous: true
requirements:
  - SYS-01
  - SYS-03
  - OPS-02
user_setup: []
must_haves:
  truths:
    - 系统页成为 gateway 设置、catalog 扫描根、共享管理员、维护动作和高级路径总览的正式 UI 入口。
    - 系统页使用 UI-SPEC 约定的人话文案、高级折叠区和显式 apply-result 状态。
    - 旧的独立管理员导航不再和系统页这个正式真源入口竞争。
  artifacts:
    - `webui/src/views/SystemView.vue` 承载新的 Phase 1 控制面。
    - `webui/src/router.ts` 把 `/config/admins` 重定向到 `/system`。
    - `webui/src/components/AppSidebar.vue` 不再单独暴露管理员页面入口。
    - `tests/runtime/test_webui_api.py` 校验新的源码结构和系统页标记。
  key_links:
    - `SystemView.vue` -> `GET /api/system/configuration`
    - `SystemView.vue` -> 各分区现有保存 endpoint，并映射 apply-result
    - `AppSidebar.vue` 与 `router.ts` 一起体现 `system` 作为共享配置入口
---

<objective>
把 Phase 1 的后端契约和列表编辑模式真正接进系统页。

目的：让系统页成为 AcaBot 值得信任的共享配置控制面。
产出：重构后的 `SystemView`、导航收敛、缓存安全的数据流，以及端到端的源码/构建回归覆盖。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-runtime-paths/01-CONTEXT.md
@.planning/phases/01-system-runtime-paths/01-RESEARCH.md
@.planning/phases/01-system-runtime-paths/01-UI-SPEC.md
@.planning/phases/01-system-runtime-paths/01-01-PLAN.md
@.planning/phases/01-system-runtime-paths/01-02-PLAN.md
@webui-pages-draft.md
@webui/src/views/SystemView.vue
@webui/src/views/AdminsView.vue
@webui/src/components/EditableListField.vue
@webui/src/router.ts
@webui/src/components/AppSidebar.vue
@webui/src/lib/api.ts
@tests/runtime/test_webui_api.py
</context>

<tasks>

<task type="auto">
  <name>任务 1：把 SystemView 重构成共享系统配置面</name>
  <files>webui/src/views/SystemView.vue, webui/src/lib/api.ts</files>
  <read_first>webui/src/views/SystemView.vue, webui/src/lib/api.ts, webui-pages-draft.md, .planning/phases/01-system-runtime-paths/01-UI-SPEC.md, .planning/phases/01-system-runtime-paths/01-RESEARCH.md</read_first>
  <action>重构 `webui/src/views/SystemView.vue`，让它通过 `GET /api/system/configuration` 加载系统配置快照，并渲染 gateway 设置、catalog 扫描根编辑器、共享管理员编辑器、维护动作区，以及一个“高级信息 / 路径总览”的折叠区。管理员 ID 和扫描根都必须复用 `EditableListField.vue`。把后端 apply-result 语义映射成 `ds-status` 消息，严格遵守 context 锁定的四态模型：`已保存并已生效`、`已保存，但需重启`、`保存失败`、`已写入，但应用失败`。同时更新 `webui/src/lib/api.ts` 的缓存失效逻辑，使 `/api/gateway/config`、`/api/filesystem/config` 或 `/api/admins` 保存后，也会同步失效 `/api/system/configuration`。</action>
  <verify>rg -n "/api/system/configuration|EditableListField|高级信息|路径总览|保存并尝试生效|已保存，需要重启后生效|apply_status" webui/src/views/SystemView.vue webui/src/lib/api.ts</verify>
  <acceptance_criteria>
    - `webui/src/views/SystemView.vue` 包含精确字符串 `/api/system/configuration`
    - `webui/src/views/SystemView.vue` 包含精确字符串 `EditableListField`
    - `webui/src/views/SystemView.vue` 包含精确字符串 `高级信息`
    - `webui/src/views/SystemView.vue` 包含精确字符串 `路径总览`
    - `webui/src/lib/api.ts` 包含精确字符串 `/api/system/configuration`
  </acceptance_criteria>
  <done>SystemView 已成为真正的 Phase 1 配置页，并对分区保存具备缓存安全性。</done>
</task>

<task type="auto">
  <name>任务 2：让 system 成为共享管理员/系统配置的唯一导航入口</name>
  <files>webui/src/router.ts, webui/src/components/AppSidebar.vue</files>
  <read_first>webui/src/router.ts, webui/src/components/AppSidebar.vue, webui-pages-draft.md, .planning/phases/01-system-runtime-paths/01-CONTEXT.md</read_first>
  <action>更新 WebUI 导航，让共享系统/管理员配置统一从 `/system` 被发现。在 `webui/src/router.ts` 中，把 `/config/admins` 从独立页面路由改成重定向到 `/system`。在 `webui/src/components/AppSidebar.vue` 中，移除单独的 `管理员` 导航项，同时保持其余页面树结构稳定。除非仓库有直接需要，否则不要删除现有文件；路由兼容性优先于文件删除。</action>
  <verify>rg -n '"/config/admins"|redirect: "/system"|管理员' webui/src/router.ts webui/src/components/AppSidebar.vue</verify>
  <acceptance_criteria>
    - `webui/src/router.ts` 包含精确字符串 `redirect: "/system"`
    - `webui/src/components/AppSidebar.vue` 不包含精确字符串 `to="/config/admins"`
    - `webui/src/components/AppSidebar.vue` 仍然包含精确字符串 `to="/system"`
  </acceptance_criteria>
  <done>导航不再呈现一个和系统页竞争的独立管理员配置入口。</done>
</task>

<task type="auto">
  <name>任务 3：为 Phase 1 系统页补最终回归覆盖和构建验证</name>
  <files>tests/runtime/test_webui_api.py</files>
  <read_first>tests/runtime/test_webui_api.py, webui/src/views/SystemView.vue, webui/src/router.ts, webui/src/components/AppSidebar.vue</read_first>
  <action>在 `tests/runtime/test_webui_api.py` 中增加源码级断言，验证重构后的系统页包含高级区标记、新 endpoint 字符串和列表编辑器使用痕迹，并验证 router/sidebar 已经把共享管理员配置统一收口到 `/system`。断言风格保持与现有 WebUI 源码测试一致。把仓库构建命令 `npm --prefix webui run build` 作为本计划的构建验证门。</action>
  <verify>npm --prefix webui run build && PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "SystemView or webui_real_pages or admins or filesystem"</verify>
  <acceptance_criteria>
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `/api/system/configuration`
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `高级信息`
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `redirect: "/system"`
    - `npm --prefix webui run build && PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "SystemView or webui_real_pages or admins or filesystem"` 退出码为 0
  </acceptance_criteria>
  <done>重构后的系统页及其导航模型已有构建和回归检查保护。</done>
</task>

</tasks>

<verification>
在声明计划完成之前：
- [ ] `npm --prefix webui run build && PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "SystemView or webui_real_pages or admins or filesystem"` 通过
- [ ] `rg -n "/api/system/configuration|高级信息|路径总览|redirect: \"/system\"" webui/src/views/SystemView.vue webui/src/router.ts webui/src/components/AppSidebar.vue` 能看到集成标记
- [ ] 系统页保持在现有 `ds-*` 设计系统语言内
</verification>

<success_criteria>

- 所有任务完成
- 所有验证检查通过
- 没有引入新的错误或告警
- 系统页成为共享系统/管理员配置的唯一正式 UI 入口
- 高级诊断能够解释路径真源，但不会把原始配置黑话直接当主标签暴露
  </success_criteria>

<output>
完成后创建 `.planning/phases/01-system-runtime-paths/01-03-SUMMARY.md`
</output>
