---
phase: 01-system-runtime-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webui/src/components/EditableListField.vue
  - webui/src/views/AdminsView.vue
  - tests/runtime/test_webui_api.py
autonomous: true
requirements:
  - SYS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - Phase 1 的共享列表编辑不再依赖多行 textarea 解析。
    - 管理员 ID 可以通过可复用的单项编辑器完成添加、去重、移除和保存。
    - 这个可复用编辑器必须符合 Phase 1 的 UI 契约和现有设计系统。
  artifacts:
    - `webui/src/components/EditableListField.vue` 作为可复用列表编辑器存在。
    - `webui/src/views/AdminsView.vue` 使用该编辑器，而不是靠换行拆分。
    - `tests/runtime/test_webui_api.py` 包含管理员页迁移后的源码级断言。
  key_links:
    - `EditableListField.vue` -> `AdminsView.vue`
    - 管理员页保存流程继续以显式数组 payload 调用 `/api/admins`
---

<objective>
建立 Phase 1 需要的可复用列表编辑交互。

目的：在系统页正式接管共享管理员和扫描根编辑之前，先落定“一次编辑一个条目”的交互模式。
产出：可复用列表编辑组件、迁移后的 `AdminsView` 参考实现，以及对应的回归检查。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-system-runtime-paths/01-CONTEXT.md
@.planning/phases/01-system-runtime-paths/01-RESEARCH.md
@.planning/phases/01-system-runtime-paths/01-UI-SPEC.md
@webui/src/styles/design-system.css
@webui/src/views/AdminsView.vue
@webui/src/views/SystemView.vue
@tests/runtime/test_webui_api.py
</context>

<tasks>

<task type="auto">
  <name>任务 1：实现可复用的单项列表编辑器组件</name>
  <files>webui/src/components/EditableListField.vue</files>
  <read_first>webui/src/styles/design-system.css, webui/src/views/AdminsView.vue, .planning/phases/01-system-runtime-paths/01-UI-SPEC.md</read_first>
  <action>创建 `webui/src/components/EditableListField.vue`，作为 Phase 1 的可复用列表编辑器。组件必须支持：可见标签、辅助说明、一条文本输入框、回车添加、点击按钮添加、重复项拦截、已添加条目的渲染与移除，以及空状态展示。必须复用现有 `ds-*` class，并保持和 glass-console 设计系统一致的视觉语言。主交互不能用 textarea。批量导入可以保留为可选扩展，但组件 API 不能阻碍后续补上这个能力。</action>
  <verify>rg -n "defineProps|Enter|empty|remove|duplicate|ds-" webui/src/components/EditableListField.vue</verify>
  <acceptance_criteria>
    - `webui/src/components/EditableListField.vue` 存在
    - `webui/src/components/EditableListField.vue` 包含 `defineProps`
    - `webui/src/components/EditableListField.vue` 包含 `@keydown.enter`
    - `webui/src/components/EditableListField.vue` 包含精确字符串 `ds-field`
    - `webui/src/components/EditableListField.vue` 不包含精确字符串 `textarea class="ds-textarea ds-mono"`
  </acceptance_criteria>
  <done>可复用列表编辑器已经存在，并支持 Phase 1 需要的单项编辑交互模式。</done>
</task>

<task type="auto">
  <name>任务 2：把 AdminsView 迁移到可复用列表编辑器和数组草稿流</name>
  <files>webui/src/views/AdminsView.vue</files>
  <read_first>webui/src/views/AdminsView.vue, webui/src/components/EditableListField.vue, .planning/phases/01-system-runtime-paths/01-CONTEXT.md</read_first>
  <action>重构 `webui/src/views/AdminsView.vue`，让它以数组草稿形式持有 `admin_actor_ids`，并通过 `EditableListField.vue` 渲染编辑交互。移除基于换行 textarea 的工作流，以及 `split("\\n").map(...).filter(Boolean)` 这条保存路径。保留现有的加载/保存状态消息模式，保持路由仍可用，并且向 `/api/admins` 提交显式的 `admin_actor_ids` 数组。</action>
  <verify>rg -n "EditableListField|admin_actor_ids|apiPut|split\\(\"\\\\n\"\\)|ds-status" webui/src/views/AdminsView.vue</verify>
  <acceptance_criteria>
    - `webui/src/views/AdminsView.vue` 包含精确字符串 `EditableListField`
    - `webui/src/views/AdminsView.vue` 包含精确字符串 `admin_actor_ids`
    - `webui/src/views/AdminsView.vue` 不包含精确字符串 `split("\\n")`
    - `webui/src/views/AdminsView.vue` 不包含精确字符串 `<textarea class="ds-textarea ds-mono"`
  </acceptance_criteria>
  <done>AdminsView 已成为单项列表编辑模式的参考实现。</done>
</task>

<task type="auto">
  <name>任务 3：为新的列表编辑模式补源码级回归检查</name>
  <files>tests/runtime/test_webui_api.py</files>
  <read_first>tests/runtime/test_webui_api.py, webui/src/views/AdminsView.vue, webui/src/components/EditableListField.vue</read_first>
  <action>在 `tests/runtime/test_webui_api.py` 中补一组轻量级源码断言，验证 `AdminsView.vue` 已经导入并使用 `EditableListField`，不再包含旧的多行 textarea 模式，同时仍然纳入现有共享设计系统的检查逻辑。断言风格保持与该文件现有源码级测试一致。</action>
  <verify>PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "AdminsView or webui_real_pages"</verify>
  <acceptance_criteria>
    - `tests/runtime/test_webui_api.py` 包含精确字符串 `EditableListField`
    - `PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "AdminsView or webui_real_pages"` 退出码为 0
  </acceptance_criteria>
  <done>新的列表编辑模式已有仓库内的回归测试保护。</done>
</task>

</tasks>

<verification>
在声明计划完成之前：
- [ ] `PYTHONPATH=src pytest tests/runtime/test_webui_api.py -q -k "AdminsView or webui_real_pages"` 通过
- [ ] `rg -n "EditableListField|split\\(\"\\\\n\"\\)" webui/src/views/AdminsView.vue` 能看到新组件并确认已去掉换行解析
- [ ] `webui/src/components/EditableListField.vue` 保持在现有 `ds-*` 设计系统语言内
</verification>

<success_criteria>

- 所有任务完成
- 所有验证检查通过
- 没有引入新的错误或告警
- 共享列表编辑不再依赖 textarea 解析
- AdminsView 展示出 Phase 1 需要的精确交互样式
  </success_criteria>

<output>
完成后创建 `.planning/phases/01-system-runtime-paths/01-02-SUMMARY.md`
</output>
