---
phase: 06-runtime-infra-artifact-backfill
plan: 4
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md
  - .planning/phases/06-runtime-infra-artifact-backfill/06-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
  - SCHED-07
  - SCHED-08
  - LTM-01
  - LTM-02
  - LTM-03
  - LTM-04
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
must_haves:
  truths:
    - "Phase 06 自己有 phase-level `06-VERIFICATION.md`，能汇总 3a / 3b / 3c 的当前证据，而不是只留分散 summary。"
    - "`06-VALIDATION.md` 不再停在 draft + Wave 0 未完成，Phase 06 close-out 状态能被当前 GSD 流程消费。"
    - "`REQUIREMENTS.md`、`STATE.md`、`v1.0-MILESTONE-AUDIT.md` 一起反映 runtime infra orphan gap 已收掉。"
  artifacts:
    - path: ".planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md"
      provides: "Phase 06 aggregated verification report for 3a / 3b / 3c"
    - path: ".planning/phases/06-runtime-infra-artifact-backfill/06-VALIDATION.md"
      provides: "Phase 06 close-out validation report with no remaining Wave 0 gap"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 06 requirements status moved from pending to validated"
    - path: ".planning/STATE.md"
      provides: "Phase 06 planned/executed state and milestone routing updated"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "Re-run audit report with runtime infra orphan gap removed"
  key_links:
    - from: ".planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md"
      to: ".planning/phases/3a-scheduler/3a-VERIFICATION.md"
      via: "Phase 06 summary references current scheduler evidence"
      pattern: "3a-VERIFICATION.md|SCHED-01"
    - from: ".planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md"
      to: ".planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md"
      via: "Phase 06 summary references current LTM evidence"
      pattern: "3b-VERIFICATION.md|LTM-01"
    - from: ".planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md"
      to: ".planning/phases/3c-logging-observability/3c-VERIFICATION.md"
      via: "Phase 06 summary references current logging evidence"
      pattern: "3c-VERIFICATION.md|LOG-01"
    - from: ".planning/v1.0-MILESTONE-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "audit rerun reflects validated runtime infra requirements"
      pattern: "SCHED-01|LTM-01|LOG-01"
---

<objective>
Close Phase 06 for real after `06-01`, `06-02`, and `06-03` finish, so runtime infra backfill stops at a clean audit-visible end state instead of dangling in planning-only artifacts.

Purpose: turn the reconstructed `3a` / `3b` / `3c` evidence into a phase-level close-out, update top-level traceability, and prove the runtime infra orphan gap is gone in a fresh milestone audit.
Output: `06-VERIFICATION.md`, finalized `06-VALIDATION.md`, synced `REQUIREMENTS.md` / `STATE.md`, and a refreshed milestone audit report with the runtime infra orphan gap removed.
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-01-PLAN.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-02-PLAN.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-03-PLAN.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-VALIDATION.md
@.planning/phases/3a-scheduler/3a-VERIFICATION.md
@.planning/phases/3a-scheduler/3a-VALIDATION.md
@.planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md
@.planning/phases/3b-ltm-data-safety/3b-VALIDATION.md
@.planning/phases/3c-logging-observability/3c-VERIFICATION.md
@.planning/phases/3c-logging-observability/3c-VALIDATION.md

<interfaces>
From `.planning/REQUIREMENTS.md`:
```md
| SCHED-01 | 6 | Pending |
| LOG-01 | 6 | Pending |
| LTM-01 | 6 | Pending |
```

From `.planning/STATE.md`:
```md
| 6 | Runtime Infra Artifact Backfill | **Queued ⏳** | SCHED-01..08, LTM-01..04, LOG-01..06 |
- Phase 06 not yet planned or executed: Phase 3a / 3b / 3c evidence chain still needs real GSD closure
```

From `.planning/v1.0-MILESTONE-AUDIT.md`:
```md
- id: "SCHED-01..08"
  status: "orphaned"
- id: "LTM-01..04"
  status: "orphaned"
- id: "LOG-01..06"
  status: "orphaned"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate 3a / 3b / 3c evidence into a phase-level Phase 06 verification report</name>
  <read_first>
.planning/phases/3a-scheduler/3a-VERIFICATION.md
.planning/phases/3a-scheduler/3a-VALIDATION.md
.planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md
.planning/phases/3b-ltm-data-safety/3b-VALIDATION.md
.planning/phases/3c-logging-observability/3c-VERIFICATION.md
.planning/phases/3c-logging-observability/3c-VALIDATION.md
  </read_first>
  <files>.planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md, .planning/phases/06-runtime-infra-artifact-backfill/06-VALIDATION.md</files>
  <acceptance_criteria>
`test -f .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md` succeeds.
`rg -n "^status: passed$" .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md` returns one hit.
`rg -n "3a-VERIFICATION.md|3b-VERIFICATION.md|3c-VERIFICATION.md|SCHED-01|LTM-01|LOG-01" .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md` returns the aggregated evidence map.
`rg -n "^status: passed$|^wave_0_complete: true$|^nyquist_compliant: true$" .planning/phases/06-runtime-infra-artifact-backfill/06-VALIDATION.md` returns the finalized close-out fields.
  </acceptance_criteria>
  <action>Write `06-VERIFICATION.md` as the phase-level report that explicitly summarizes which current commands and backfilled artifacts closed `3a`, `3b`, and `3c`. This report should not duplicate every line from the underlying phase reports; it should point to the original phase artifacts and summarize the evidence chain that now exists because of `06-01`, `06-02`, and `06-03`. Then finalize `06-VALIDATION.md`: replace the current draft / Wave 0 placeholders with the actual passing command map from the three completed plans, mark `status: passed`, set `wave_0_complete: true`, keep `nyquist_compliant: true`, and remove any wording that leaves Phase 06 hanging as "pending".</action>
  <verify>
    <automated>test -f .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md && rg -n "^status: passed$" .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md && rg -n "3a-VERIFICATION.md|3b-VERIFICATION.md|3c-VERIFICATION.md|SCHED-01|LTM-01|LOG-01" .planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md && rg -n "^status: passed$|^wave_0_complete: true$|^nyquist_compliant: true$" .planning/phases/06-runtime-infra-artifact-backfill/06-VALIDATION.md</automated>
  </verify>
  <done>Phase 06 has its own audit-readable verification and validation close-out, and both files clearly show that the runtime infra evidence chain is now complete instead of draft-only.</done>
</task>

<task type="auto">
  <name>Task 2: Sync top-level traceability and rerun milestone audit until the runtime infra orphan gap disappears</name>
  <read_first>
.planning/REQUIREMENTS.md
.planning/STATE.md
.planning/v1.0-MILESTONE-AUDIT.md
.planning/phases/06-runtime-infra-artifact-backfill/06-VERIFICATION.md
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <acceptance_criteria>
`rg -n "\\| SCHED-01 \\| 6 \\| Validated \\||\\| LOG-01 \\| 6 \\| Validated \\||\\| LTM-01 \\| 6 \\| Validated \\|" .planning/REQUIREMENTS.md` returns validated Phase 06 traceability rows.
`rg -n "Runtime Infra Artifact Backfill \\| \\*\\*(Executed|Verified) ✅\\*\\*|Phase 06 .*closure" .planning/STATE.md` returns the updated phase status and blocker/routing cleanup.
`codex exec --sandbox danger-full-access -a never -C /home/acacia/AcaBot '$gsd-audit-milestone v1.0'` succeeds and regenerates `.planning/v1.0-MILESTONE-AUDIT.md` before any content assertions run.
`awk '
  BEGIN {
    wanted["SCHED-01..08"] = 1
    wanted["LTM-01..04"] = 1
    wanted["LOG-01..06"] = 1
  }
  /^    - id: "/ {
    current = $0
    sub(/^    - id: "/, "", current)
    sub(/"$/, "", current)
    in_target = (current in wanted)
    if (in_target) {
      seen[current] = 1
      status[current] = ""
      completed[current] = ""
      verification[current] = ""
    }
    next
  }
  in_target && /^      status: "/ {
    status[current] = $0
    sub(/^      status: "/, "", status[current])
    sub(/"$/, "", status[current])
    next
  }
  in_target && /^      completed_by_plans: / {
    completed[current] = $0
    next
  }
  in_target && /^      verification_status: "/ {
    verification[current] = $0
    sub(/^      verification_status: "/, "", verification[current])
    sub(/"$/, "", verification[current])
    next
  }
  END {
    for (id in wanted) {
      if (!seen[id]) {
        printf("missing audit block for %s\n", id) > "/dev/stderr"
        exit 1
      }
      if (status[id] == "orphaned") {
        printf("%s still orphaned\n", id) > "/dev/stderr"
        exit 1
      }
      if (completed[id] == "      completed_by_plans: []") {
        printf("%s still has empty completed_by_plans\n", id) > "/dev/stderr"
        exit 1
      }
      if (verification[id] == "missing") {
        printf("%s still has missing verification_status\n", id) > "/dev/stderr"
        exit 1
      }
    }
  }
' .planning/v1.0-MILESTONE-AUDIT.md` succeeds, proving each runtime infra requirement group still has an audit entry but no longer carries the orphan signature.
  </acceptance_criteria>
  <action>Update `.planning/REQUIREMENTS.md` so all `SCHED-01..08`, `LTM-01..04`, and `LOG-01..06` rows for Phase 06 move from `Pending` to `Validated`, and keep the phase mapping at `6`. Update `.planning/STATE.md` so Phase 06 is no longer queued, the active routing advances past runtime infra backfill, and the blocker text about missing `3a / 3b / 3c` evidence is removed or replaced with completed close-out language. Then rerun the milestone audit with the local GSD workflow and write the refreshed `.planning/v1.0-MILESTONE-AUDIT.md`. The rerun must prove that the runtime infra orphan gap is gone; if the audit still flags these groups, keep iterating on traceability mismatches in `06-VERIFICATION.md`, `REQUIREMENTS.md`, or `STATE.md` until the audit report stops classifying `SCHED-01..08`, `LTM-01..04`, and `LOG-01..06` as orphaned.</action>
  <verify>
    <automated>bash -lc 'codex exec --sandbox danger-full-access -a never -C /home/acacia/AcaBot '\''$gsd-audit-milestone v1.0'\'' && rg -n "\\| SCHED-01 \\| 6 \\| Validated \\||\\| LOG-01 \\| 6 \\| Validated \\||\\| LTM-01 \\| 6 \\| Validated \\|" .planning/REQUIREMENTS.md && rg -n "Runtime Infra Artifact Backfill \\| \\*\\*(Executed|Verified) ✅\\*\\*|Phase 06 .*closure" .planning/STATE.md && awk '\''BEGIN { wanted["SCHED-01..08"] = 1; wanted["LTM-01..04"] = 1; wanted["LOG-01..06"] = 1 } /^    - id: "/ { current = $0; sub(/^    - id: "/, "", current); sub(/"$/, "", current); in_target = (current in wanted); if (in_target) { seen[current] = 1; status[current] = ""; completed[current] = ""; verification[current] = "" } next } in_target && /^      status: "/ { status[current] = $0; sub(/^      status: "/, "", status[current]); sub(/"$/, "", status[current]); next } in_target && /^      completed_by_plans: / { completed[current] = $0; next } in_target && /^      verification_status: "/ { verification[current] = $0; sub(/^      verification_status: "/, "", verification[current]); sub(/"$/, "", verification[current]); next } END { for (id in wanted) { if (!seen[id]) { printf("missing audit block for %s\n", id) > "/dev/stderr"; exit 1 } if (status[id] == "orphaned") { printf("%s still orphaned\n", id) > "/dev/stderr"; exit 1 } if (completed[id] == "      completed_by_plans: []") { printf("%s still has empty completed_by_plans\n", id) > "/dev/stderr"; exit 1 } if (verification[id] == "missing") { printf("%s still has missing verification_status\n", id) > "/dev/stderr"; exit 1 } } }'\'' .planning/v1.0-MILESTONE-AUDIT.md'</automated>
  </verify>
  <done>Top-level planning state matches the new runtime infra evidence, and a fresh milestone audit no longer reports the Phase 3a / 3b / 3c requirements as orphaned.</done>
</task>

</tasks>

<verification>
Check that `06-VERIFICATION.md` exists and has `status: passed`.
Check that `06-VALIDATION.md` has `status: passed`, `wave_0_complete: true`, and `nyquist_compliant: true`.
Run the requirement/state/audit grep from Task 2 after the milestone audit rerun completes.
</verification>

<success_criteria>
Phase 06 no longer ends at three evidence-rebuild plans with no close-out.
`06-VERIFICATION.md` and finalized `06-VALIDATION.md` summarize the runtime infra backfill honestly.
`REQUIREMENTS.md`, `STATE.md`, and the refreshed milestone audit all agree that the runtime infra orphan gap has been removed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-infra-artifact-backfill/06-04-SUMMARY.md`
</output>
