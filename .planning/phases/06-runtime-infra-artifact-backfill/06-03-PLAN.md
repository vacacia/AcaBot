---
phase: 06-runtime-infra-artifact-backfill
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/acabot/agent/agent.py
  - src/acabot/agent/response.py
  - src/acabot/runtime/pipeline.py
  - tests/test_agent.py
  - tests/runtime/test_pipeline_runtime.py
  - tests/runtime/test_webui_api.py
  - tests/runtime/test_long_term_memory_source.py
  - .planning/phases/3c-logging-observability/3c-01-SUMMARY.md
  - .planning/phases/3c-logging-observability/3c-02-SUMMARY.md
  - .planning/phases/3c-logging-observability/3c-03-SUMMARY.md
  - .planning/phases/3c-logging-observability/3c-VERIFICATION.md
  - .planning/phases/3c-logging-observability/3c-VALIDATION.md
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
must_haves:
  truths:
    - "Per-run token logs include `model`, token breakdown, and `cost_usd`, and the same cost is persisted on the run record."
    - "WebUI logs page has an automated proof that structured `extra` fields render as chips in the real page."
    - "3c 原目录里有三份 plan-level summary 和完整 verification/validation, 能把 LOG-01..06 回填到原 phase。"
  artifacts:
    - path: "src/acabot/agent/agent.py"
      provides: "LOG-02 cost-aware structured usage logs"
    - path: "src/acabot/runtime/pipeline.py"
      provides: "run metadata persistence for token usage + cost"
    - path: "tests/runtime/test_webui_api.py"
      provides: "LOG-04 browser proof"
    - path: "tests/runtime/test_long_term_memory_source.py"
      provides: "LOG-05 extraction/query structured log proof"
    - path: ".planning/phases/3c-logging-observability/3c-VERIFICATION.md"
      provides: "logging verification report"
    - path: ".planning/phases/3c-logging-observability/3c-VALIDATION.md"
      provides: "logging Nyquist validation map"
  key_links:
    - from: "src/acabot/agent/agent.py"
      to: "src/acabot/runtime/pipeline.py"
      via: "AgentResponse carries cost_usd into run metadata"
      pattern: "cost_usd"
    - from: "tests/runtime/test_webui_api.py"
      to: "webui/src/components/LogStreamPanel.vue"
      via: "browser assertion for extra chips"
      pattern: "extra-chip|log-extra"
    - from: ".planning/phases/3c-logging-observability/3c-02-SUMMARY.md"
      to: "LOG-02"
      via: "requirements-completed frontmatter"
      pattern: "LOG-02"
---

<objective>
Backfill the logging and observability evidence chain into the original `3c-logging-observability/` directory and close the remaining proof gaps for `cost`, WebUI `extra` rendering, and LTM extraction logs.

Purpose: let `LOG-01..06` pass audit honestly, with one small runtime fix for cost propagation and the missing automated UI/extraction proofs.
Output: cost-aware structured logging changes, new browser/log tests, and `3c-01/02/03-SUMMARY.md`, `3c-VERIFICATION.md`, `3c-VALIDATION.md`.
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
@.planning/phases/3c-logging-observability/3c-CONTEXT.md
@.planning/phases/3c-logging-observability/3c-PLAN-01.md
@.planning/phases/3c-logging-observability/3c-PLAN-02.md
@.planning/phases/3c-logging-observability/3c-PLAN-03.md
@src/acabot/agent/agent.py
@src/acabot/agent/response.py
@src/acabot/runtime/pipeline.py
@src/acabot/runtime/control/log_buffer.py
@src/acabot/runtime/control/log_setup.py
@src/acabot/runtime/memory/long_term_memory/model_clients.py
@webui/src/components/LogStreamPanel.vue
@tests/test_agent.py
@tests/test_structured_logging.py
@tests/runtime/test_tool_broker.py
@tests/runtime/test_pipeline_runtime.py
@tests/runtime/test_webui_api.py
@tests/runtime/test_long_term_memory_source.py

<interfaces>
From `src/acabot/agent/response.py`:
```python
@dataclass
class AgentResponse:
    text: str = ""
    usage: dict[str, int] = field(default_factory=dict)
    model_used: str = ""
    raw: Any = None
```

From `src/acabot/runtime/pipeline.py`:
```python
async def _finish_run(self, ctx: RunContext) -> None:
    if response.usage:
        ctx.run.metadata["token_usage"] = dict(response.usage)
        slog.info("Run token usage", model=model_used, prompt_tokens=..., completion_tokens=..., total_tokens=...)
```

From `webui/src/components/LogStreamPanel.vue`:
```ts
type LogItem = { seq: number; timestamp: number; level: string; logger: string; message: string; kind?: string; extra?: Record<string, unknown> }
function applyExtraFilter(key: string, value: unknown): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cost-aware token logging and run persistence for LOG-02</name>
  <read_first>
src/acabot/agent/agent.py
src/acabot/agent/response.py
src/acabot/runtime/pipeline.py
tests/test_agent.py
tests/runtime/test_pipeline_runtime.py
  </read_first>
  <files>src/acabot/agent/agent.py, src/acabot/agent/response.py, src/acabot/runtime/pipeline.py, tests/test_agent.py, tests/runtime/test_pipeline_runtime.py</files>
  <acceptance_criteria>
`rg -n "cost_usd" src/acabot/agent/agent.py src/acabot/agent/response.py src/acabot/runtime/pipeline.py tests/test_agent.py tests/runtime/test_pipeline_runtime.py` returns hits in all five files.
`rg -n "usage_cost_usd|Run token usage" src/acabot/runtime/pipeline.py tests/runtime/test_pipeline_runtime.py` shows both persistence and log assertions.
  </acceptance_criteria>
  <behavior>
    - Test 1: agent `run()` emits a structured usage log containing `model`, token counts, and `cost_usd` when `litellm.completion_cost()` returns a value.
    - Test 2: agent `complete()` emits the same `cost_usd` field without changing existing token-count behavior.
    - Test 3: `ThreadPipeline._finish_run()` persists the computed cost on the run metadata and includes `cost_usd` in the `Run token usage` structured log.
  </behavior>
  <action>Add a `cost_usd: float | None` field to `AgentResponse`, implement a best-effort helper in `src/acabot/agent/agent.py` that calls LiteLLM's built-in `completion_cost(completion_response=response, model=use_model)` and rounds it for log/persistence use, then propagate that value through both `run()` and `complete()`. In `src/acabot/runtime/pipeline.py`, persist the value as `ctx.run.metadata["usage_cost_usd"]` and include `cost_usd` in the structured `Run token usage` log. If cost lookup raises because a model has no pricing data, swallow the error and keep the agent run successful; the new tests should patch the helper or `completion_cost` so the proof path is deterministic.</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/test_agent.py tests/runtime/test_pipeline_runtime.py -k 'structured_usage_log or token_usage_and_logs_it'</automated>
  </verify>
  <done>`LOG-02` is no longer partial: both agent-level logs and persisted run metadata carry a deterministic `cost_usd` field in automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add direct automated proof for LOG-04 and the missing extraction side of LOG-05</name>
  <read_first>
webui/src/components/LogStreamPanel.vue
src/acabot/runtime/memory/long_term_memory/model_clients.py
tests/runtime/test_webui_api.py
tests/runtime/test_long_term_memory_source.py
  </read_first>
  <files>tests/runtime/test_webui_api.py, tests/runtime/test_long_term_memory_source.py</files>
  <acceptance_criteria>
`rg -n "test_logs_page_renders_structured_extra_fields|test_extractor_client_emits_structured_log" tests/runtime/test_webui_api.py tests/runtime/test_long_term_memory_source.py` returns both new proof tests.
`rg -n "extra-chip|LTM extraction completed" tests/runtime/test_webui_api.py tests/runtime/test_long_term_memory_source.py` returns the DOM/log assertions that close the audit gap.
  </acceptance_criteria>
  <action>Add one browser test in `tests/runtime/test_webui_api.py` that appends a `LogEntry` with `extra={"tool_name": "echo", "duration_ms": 42, "run_id": "run:1"}`, opens `/logs`, and asserts the page renders `.extra-chip` elements containing the structured key/value text from `LogStreamPanel.vue`. Add one log test in `tests/runtime/test_long_term_memory_source.py` that drives `LtmExtractorClient.extract_window()`, captures `acabot.runtime.memory.long_term_memory.model_clients`, and asserts the `LTM extraction completed` record includes `conversation_id`, `entry_count`, and `duration_ms`. Only touch production code if these tests expose a real mismatch; current source already suggests both behaviors should exist.</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_webui_api.py tests/runtime/test_long_term_memory_source.py -k 'structured_extra_fields or extraction completed or logs_page'</automated>
  </verify>
  <done>The WebUI `extra` chip rendering and LTM extraction structured logging both have direct automated proof, not just inferred coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill 3c summaries, verification, and validation in the original logging phase directory</name>
  <read_first>
.planning/phases/3c-logging-observability/3c-PLAN-01.md
.planning/phases/3c-logging-observability/3c-PLAN-02.md
.planning/phases/3c-logging-observability/3c-PLAN-03.md
.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
.planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <files>.planning/phases/3c-logging-observability/3c-01-SUMMARY.md, .planning/phases/3c-logging-observability/3c-02-SUMMARY.md, .planning/phases/3c-logging-observability/3c-03-SUMMARY.md, .planning/phases/3c-logging-observability/3c-VERIFICATION.md, .planning/phases/3c-logging-observability/3c-VALIDATION.md</files>
  <acceptance_criteria>
`rg -n "requirements-completed: \\[LOG-03, LOG-06\\]" .planning/phases/3c-logging-observability/3c-01-SUMMARY.md` returns one hit.
`rg -n "requirements-completed: \\[LOG-01, LOG-02, LOG-05\\]" .planning/phases/3c-logging-observability/3c-02-SUMMARY.md` returns one hit.
`rg -n "requirements-completed: \\[LOG-04\\]" .planning/phases/3c-logging-observability/3c-03-SUMMARY.md` returns one hit.
`rg -n "^status: passed$" .planning/phases/3c-logging-observability/3c-VERIFICATION.md` returns one hit.
`rg -n "^nyquist_compliant: true$" .planning/phases/3c-logging-observability/3c-VALIDATION.md` returns one hit.
  </acceptance_criteria>
  <action>Create three plan-level summaries in `3c-logging-observability/` that mirror the original phase split: `3c-01` for structlog/context propagation, `3c-02` for emit sites plus the new `cost_usd` proof, and `3c-03` for WebUI structured-log rendering. Then write `3c-VERIFICATION.md` using the passing tests from `tests/test_structured_logging.py`, `tests/runtime/test_tool_broker.py`, `tests/test_agent.py`, `tests/runtime/test_pipeline_runtime.py`, `tests/runtime/test_webui_api.py`, and `tests/runtime/test_long_term_memory_source.py`. Finally, write `3c-VALIDATION.md` with a per-task verification map that explicitly names the new cost, UI, and extraction commands so audit no longer sees `LOG-02`, `LOG-04`, or `LOG-05` as partial.</action>
  <verify>
    <automated>node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3c-logging-observability/3c-01-SUMMARY.md --fields requirements_completed --pick requirements_completed && node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3c-logging-observability/3c-02-SUMMARY.md --fields requirements_completed --pick requirements_completed && node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3c-logging-observability/3c-03-SUMMARY.md --fields requirements_completed --pick requirements_completed</automated>
  </verify>
  <done>The original 3c directory has a complete audit-recognized artifact chain, and each logging requirement is mapped back to a concrete plan summary plus a current verification command.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=src uv run pytest -q tests/test_structured_logging.py tests/runtime/test_tool_broker.py tests/test_agent.py tests/runtime/test_pipeline_runtime.py tests/runtime/test_webui_api.py tests/runtime/test_long_term_memory_source.py`.
Run the three `summary-extract` commands after Task 3.
Check `3c-VERIFICATION.md` and `3c-VALIDATION.md` with the Task 3 grep before writing the Phase 06 summary.
</verification>

<success_criteria>
`LOG-01..06` are traceable through `3c-01/02/03-SUMMARY.md`, `3c-VERIFICATION.md`, and `3c-VALIDATION.md`.
`LOG-02` has a real `cost_usd` field in automated logs and run metadata.
`LOG-04` and the extraction side of `LOG-05` have direct automated proof, and milestone audit can consume the 3c artifact chain from the original phase directory.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-infra-artifact-backfill/06-03-SUMMARY.md`
</output>
