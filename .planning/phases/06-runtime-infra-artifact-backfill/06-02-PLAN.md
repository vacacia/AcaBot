---
phase: 06-runtime-infra-artifact-backfill
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/runtime/test_ltm_validation.py
  - .planning/phases/3b-ltm-data-safety/3b-01-SUMMARY.md
  - .planning/phases/3b-ltm-data-safety/3b-02-SUMMARY.md
  - .planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md
  - .planning/phases/3b-ltm-data-safety/3b-VALIDATION.md
autonomous: true
requirements:
  - LTM-01
  - LTM-02
  - LTM-03
  - LTM-04
must_haves:
  truths:
    - "LTM startup validation has a negative proof path, not just a fresh-store happy path."
    - "3b 原目录里有两份 summary, 能把 LTM-01..04 回填到原始两个 plan。"
    - "3b verification + validation 能证明写锁、备份、启动校验和优雅降级都对应当前代码。"
  artifacts:
    - path: "tests/runtime/test_ltm_validation.py"
      provides: "LTM-03 negative validation proof"
    - path: ".planning/phases/3b-ltm-data-safety/3b-01-SUMMARY.md"
      provides: "LTM-01, LTM-03, LTM-04 trace"
    - path: ".planning/phases/3b-ltm-data-safety/3b-02-SUMMARY.md"
      provides: "LTM-02 trace"
    - path: ".planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md"
      provides: "LTM current verification report"
    - path: ".planning/phases/3b-ltm-data-safety/3b-VALIDATION.md"
      provides: "LTM Nyquist validation map"
  key_links:
    - from: "tests/runtime/test_ltm_validation.py"
      to: "src/acabot/runtime/memory/long_term_memory/storage.py"
      via: "validate() negative cases"
      pattern: "validate\\("
    - from: ".planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md"
      to: "tests/runtime/test_ltm_backup.py"
      via: "documented backup verification command"
      pattern: "test_ltm_backup.py"
    - from: ".planning/phases/3b-ltm-data-safety/3b-02-SUMMARY.md"
      to: "LTM-02"
      via: "requirements-completed frontmatter"
      pattern: "LTM-02"
---

<objective>
Backfill the LTM data-safety evidence chain into the original `3b-ltm-data-safety/` directory and add the missing negative validation proof for startup integrity checks.

Purpose: close the audit blocker for `LTM-01..04` with honest evidence instead of treating current happy-path tests as enough.
Output: one negative validation test file plus `3b-01/02-SUMMARY.md`, `3b-VERIFICATION.md`, and `3b-VALIDATION.md`.
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
@.planning/phases/3b-ltm-data-safety/3b-CONTEXT.md
@.planning/phases/3b-ltm-data-safety/3b-PLAN-01.md
@.planning/phases/3b-ltm-data-safety/3b-PLAN-02.md
@src/acabot/runtime/memory/long_term_memory/storage.py
@src/acabot/runtime/bootstrap/builders.py
@tests/runtime/test_ltm_write_lock.py
@tests/runtime/test_ltm_backup.py
@tests/runtime/test_ltm_data_safety.py
@tests/runtime/test_bootstrap.py
@tests/runtime/test_app.py
@tests/runtime/test_long_term_memory_source.py

<interfaces>
From `src/acabot/runtime/memory/long_term_memory/storage.py`:
```python
class LanceDbLongTermMemoryStore:
    def validate(self) -> LtmValidationResult: ...
    def backup(self, target_dir: str | Path, *, max_backups: int = 5) -> Path: ...
```

From `src/acabot/runtime/bootstrap/builders.py`:
```python
def build_long_term_memory_store(config: Config) -> LanceDbLongTermMemoryStore: ...
def build_ltm_backup_task(config: Config, *, store: LanceDbLongTermMemoryStore) -> tuple[Callable[[], Awaitable[None]], int] | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add negative validation proof for LTM startup integrity checks</name>
  <read_first>
src/acabot/runtime/memory/long_term_memory/storage.py
tests/runtime/test_ltm_write_lock.py
tests/runtime/test_bootstrap.py
  </read_first>
  <files>tests/runtime/test_ltm_validation.py</files>
  <acceptance_criteria>
`rg -n "test_validate_reports_missing_required_columns|test_validate_reports_table_read_failure" tests/runtime/test_ltm_validation.py` returns both negative tests.
`rg -n "memory_entries 表缺少必需列|failed_windows 表读取失败" tests/runtime/test_ltm_validation.py` returns the expected failure text.
  </acceptance_criteria>
  <action>Create `tests/runtime/test_ltm_validation.py` with focused negative tests for `LTM-03`. One test should monkeypatch a table object so `validate()` sees missing required columns and returns `ok=False` with a `memory_entries 表缺少必需列` error. Another should monkeypatch a table object whose `to_arrow()` raises, and assert `validate()` returns `ok=False` with a `failed_windows 表读取失败` error. Reuse the existing happy-path proof in `tests/runtime/test_ltm_write_lock.py`; do not rewrite production storage code unless the new negative tests expose a real bug.</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_ltm_validation.py tests/runtime/test_ltm_write_lock.py -k 'validate'</automated>
  </verify>
  <done>`LTM-03` has both happy-path and negative-path automated proof: fresh database validates, and corrupted schema/read failures are detected.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill plan-level LTM summaries into the original 3b directory</name>
  <read_first>
.planning/phases/3b-ltm-data-safety/3b-PLAN-01.md
.planning/phases/3b-ltm-data-safety/3b-PLAN-02.md
.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
.planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <files>.planning/phases/3b-ltm-data-safety/3b-01-SUMMARY.md, .planning/phases/3b-ltm-data-safety/3b-02-SUMMARY.md</files>
  <acceptance_criteria>
`rg -n "requirements-completed: \\[LTM-01, LTM-03, LTM-04\\]" .planning/phases/3b-ltm-data-safety/3b-01-SUMMARY.md` returns one hit.
`rg -n "requirements-completed: \\[LTM-02\\]" .planning/phases/3b-ltm-data-safety/3b-02-SUMMARY.md` returns one hit.
  </acceptance_criteria>
  <action>Create `3b-01-SUMMARY.md` for write serialization, startup validation, and graceful degradation, then create `3b-02-SUMMARY.md` for periodic backup. Follow the Phase 04 summary format and keep the wording honest: Phase 06 is reconstructing evidence for work that already shipped. Mention the new negative validation proof from Task 1 in the `3b-01` summary, and use current code/test evidence plus `git log` instead of guessed commit stories.</action>
  <verify>
    <automated>node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3b-ltm-data-safety/3b-01-SUMMARY.md --fields requirements_completed --pick requirements_completed && node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3b-ltm-data-safety/3b-02-SUMMARY.md --fields requirements_completed --pick requirements_completed</automated>
  </verify>
  <done>The original 3b directory contains one summary for `LTM-01/03/04` and one summary for `LTM-02`, with no requirement left unmapped.</done>
</task>

<task type="auto">
  <name>Task 3: Write current-code verification and Nyquist validation for Phase 3b</name>
  <read_first>
.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
tests/runtime/test_ltm_validation.py
tests/runtime/test_ltm_write_lock.py
tests/runtime/test_ltm_backup.py
tests/runtime/test_ltm_data_safety.py
tests/runtime/test_bootstrap.py
tests/runtime/test_app.py
tests/runtime/test_long_term_memory_source.py
  </read_first>
  <files>.planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md, .planning/phases/3b-ltm-data-safety/3b-VALIDATION.md</files>
  <acceptance_criteria>
`rg -n "^status: passed$" .planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md` returns one hit.
`rg -n "^nyquist_compliant: true$" .planning/phases/3b-ltm-data-safety/3b-VALIDATION.md` returns one hit.
`rg -n "test_ltm_validation.py|test_ltm_backup.py|test_ltm_data_safety.py" .planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md .planning/phases/3b-ltm-data-safety/3b-VALIDATION.md` returns the new LTM proof set.
  </acceptance_criteria>
  <action>Write `3b-VERIFICATION.md` with today's passing commands for `LTM-01`, `LTM-02`, `LTM-03`, and `LTM-04`. Use the new negative validation tests for `LTM-03`, the existing backup tests for `LTM-02`, the write-lock tests for `LTM-01`, and the bootstrap/app/source degrade tests for `LTM-04`. Then write `3b-VALIDATION.md` with a per-task map that references those commands, keeps the repo-wide full-suite command in the sign-off section, and treats the new `tests/runtime/test_ltm_validation.py` file as the Wave 0 closure for corruption proof.</action>
  <verify>
    <automated>test -f .planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md && test -f .planning/phases/3b-ltm-data-safety/3b-VALIDATION.md && rg -n "LTM-03|test_ltm_validation.py|nyquist_compliant: true" .planning/phases/3b-ltm-data-safety/3b-VERIFICATION.md .planning/phases/3b-ltm-data-safety/3b-VALIDATION.md</automated>
  </verify>
  <done>The original 3b directory has a complete audit-recognized artifact chain built from current LTM validation, backup, and degradation evidence.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=src uv run pytest -q tests/runtime/test_ltm_validation.py tests/runtime/test_ltm_write_lock.py tests/runtime/test_ltm_backup.py tests/runtime/test_ltm_data_safety.py tests/runtime/test_bootstrap.py tests/runtime/test_app.py tests/runtime/test_long_term_memory_source.py`.
Run both `summary-extract` commands after Task 2.
Check `3b-VERIFICATION.md` and `3b-VALIDATION.md` with the Task 3 grep before writing the Phase 06 summary.
</verification>

<success_criteria>
`LTM-01..04` are traceable through `3b-01/02-SUMMARY.md`, `3b-VERIFICATION.md`, and `3b-VALIDATION.md`.
There is an explicit automated negative proof that `validate()` detects corruption-style failures.
Re-running milestone audit can consume the 3b artifact chain from the original phase directory.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-infra-artifact-backfill/06-02-SUMMARY.md`
</output>
