---
phase: 06-runtime-infra-artifact-backfill
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduler_integration.py
  - .planning/phases/3a-scheduler/3a-01-SUMMARY.md
  - .planning/phases/3a-scheduler/3a-02-SUMMARY.md
  - .planning/phases/3a-scheduler/3a-VERIFICATION.md
  - .planning/phases/3a-scheduler/3a-VALIDATION.md
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
  - SCHED-07
  - SCHED-08
must_haves:
  truths:
    - "Scheduler 的 core + lifecycle 证据在今天的 RuntimeApp wiring 下可以直接复跑, 不再因为 render_service 构造路径报错。"
    - "3a 原目录里有两份 plan 级 summary, 能把 SCHED-01..08 映射回原始 plan 粒度。"
    - "3a 原目录里有 verification + validation, 并且命令指向当前真实可跑的 scheduler 证明链。"
  artifacts:
    - path: "tests/test_scheduler_integration.py"
      provides: "SCHED-08 的当前 lifecycle proof"
    - path: ".planning/phases/3a-scheduler/3a-01-SUMMARY.md"
      provides: "SCHED-01..06 的 plan-level trace"
    - path: ".planning/phases/3a-scheduler/3a-02-SUMMARY.md"
      provides: "SCHED-07..08 的 plan-level trace"
    - path: ".planning/phases/3a-scheduler/3a-VERIFICATION.md"
      provides: "scheduler 当前验证报告"
    - path: ".planning/phases/3a-scheduler/3a-VALIDATION.md"
      provides: "scheduler Nyquist 验证映射"
  key_links:
    - from: "tests/test_scheduler_integration.py"
      to: "src/acabot/runtime/app.py"
      via: "RuntimeApp lifecycle tests use a current fake pipeline/render path"
      pattern: "pipeline=.*FakePipeline|render_service="
    - from: ".planning/phases/3a-scheduler/3a-VERIFICATION.md"
      to: "tests/test_scheduler.py"
      via: "documented pytest command"
      pattern: "tests/test_scheduler.py"
    - from: ".planning/phases/3a-scheduler/3a-02-SUMMARY.md"
      to: "SCHED-08"
      via: "requirements-completed frontmatter"
      pattern: "SCHED-08"
---

<objective>
Backfill the scheduler evidence chain into the original `3a-scheduler/` directory and repair the stale lifecycle proof that broke after Phase 04 added `render_service` inference.

Purpose: close the audit blocker for `SCHED-01..08` without re-implementing scheduler behavior that already exists.
Output: refreshed `SCHED-08` tests plus `3a-01/02-SUMMARY.md`, `3a-VERIFICATION.md`, and `3a-VALIDATION.md`.
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
@.planning/phases/3a-scheduler/3a-CONTEXT.md
@.planning/phases/3a-scheduler/3a-PLAN-01.md
@.planning/phases/3a-scheduler/3a-PLAN-02.md
@src/acabot/runtime/app.py
@src/acabot/runtime/scheduler/scheduler.py
@tests/test_scheduler.py
@tests/test_scheduler_integration.py

<interfaces>
From `src/acabot/runtime/scheduler/scheduler.py`:
```python
class RuntimeScheduler:
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    async def register(...): ...
    async def cancel(self, task_id: str) -> bool: ...
    async def unregister_by_owner(self, owner: str) -> list[str]: ...
```

From `src/acabot/runtime/app.py`:
```python
class RuntimeApp:
    def __init__(..., pipeline: ThreadPipeline, scheduler: RuntimeScheduler | None = None, render_service: RenderService | None = None) -> None: ...
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the SCHED-08 lifecycle tests for current RuntimeApp wiring</name>
  <read_first>
tests/test_scheduler_integration.py
src/acabot/runtime/app.py
src/acabot/runtime/outbox.py
  </read_first>
  <files>tests/test_scheduler_integration.py</files>
  <acceptance_criteria>
`rg -n "class _FakePipeline|class _FakeOutbox" tests/test_scheduler_integration.py` returns helper definitions for the current RuntimeApp constructor.
`rg -n "pipeline=_FakePipeline\\(\\)|render_service=" tests/test_scheduler_integration.py` shows both app lifecycle tests no longer pass `pipeline=None`.
  </acceptance_criteria>
  <action>Update `tests/test_scheduler_integration.py` so `test_app_start_starts_scheduler` and `test_app_stop_order` construct `RuntimeApp` with a minimal fake pipeline/outbox or an explicit `render_service`, instead of `pipeline=None`. Keep the assertions focused on `SCHED-08`: scheduler starts during `RuntimeApp.start()`, and shutdown order remains `gateway -> scheduler -> plugin_host`. Do not modify production scheduler code here; this is a stale proof-path repair caused by the Phase 04 render-service constructor change, not a scheduler behavior regression.</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/test_scheduler_integration.py -k 'app_start_starts_scheduler or app_stop_order or unload_plugin_cancels_scheduled_tasks'</automated>
  </verify>
  <done>All three targeted scheduler integration tests pass, and the only code change is the test harness needed to satisfy current `RuntimeApp` construction.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill plan-level scheduler summaries into the original 3a directory</name>
  <read_first>
.planning/phases/3a-scheduler/3a-PLAN-01.md
.planning/phases/3a-scheduler/3a-PLAN-02.md
.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
.planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <files>.planning/phases/3a-scheduler/3a-01-SUMMARY.md, .planning/phases/3a-scheduler/3a-02-SUMMARY.md</files>
  <acceptance_criteria>
`rg -n "requirements-completed: \\[SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06\\]" .planning/phases/3a-scheduler/3a-01-SUMMARY.md` returns one hit.
`rg -n "requirements-completed: \\[SCHED-07, SCHED-08\\]" .planning/phases/3a-scheduler/3a-02-SUMMARY.md` returns one hit.
  </acceptance_criteria>
  <action>Create `3a-01-SUMMARY.md` for the core scheduler slice (`SCHED-01..06`) and `3a-02-SUMMARY.md` for plugin/app integration (`SCHED-07..08`). Follow the Phase 04 summary shape: frontmatter with `phase`, `plan`, `provides`, `affects`, `patterns-established`, and `requirements-completed`, then body sections for accomplishments, task commits, files, decisions, and deviations. Use current code + `git log` as evidence; if original Phase 3a work was folded into a shared commit, say that directly instead of inventing hashes.</action>
  <verify>
    <automated>node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3a-scheduler/3a-01-SUMMARY.md --fields requirements_completed --pick requirements_completed && node /home/acacia/.codex/get-shit-done/bin/gsd-tools.cjs summary-extract .planning/phases/3a-scheduler/3a-02-SUMMARY.md --fields requirements_completed --pick requirements_completed</automated>
  </verify>
  <done>`3a-01-SUMMARY.md` and `3a-02-SUMMARY.md` exist, and together they cover every scheduler requirement exactly in the original phase directory.</done>
</task>

<task type="auto">
  <name>Task 3: Write current-code verification and Nyquist validation for Phase 3a</name>
  <read_first>
.planning/phases/06-runtime-infra-artifact-backfill/06-RESEARCH.md
.planning/phases/3a-scheduler/3a-01-SUMMARY.md
.planning/phases/3a-scheduler/3a-02-SUMMARY.md
tests/test_scheduler.py
tests/test_scheduler_integration.py
  </read_first>
  <files>.planning/phases/3a-scheduler/3a-VERIFICATION.md, .planning/phases/3a-scheduler/3a-VALIDATION.md</files>
  <acceptance_criteria>
`rg -n "^status: passed$" .planning/phases/3a-scheduler/3a-VERIFICATION.md` returns one hit.
`rg -n "^nyquist_compliant: true$" .planning/phases/3a-scheduler/3a-VALIDATION.md` returns one hit.
`rg -n "tests/test_scheduler.py|tests/test_scheduler_integration.py" .planning/phases/3a-scheduler/3a-VERIFICATION.md .planning/phases/3a-scheduler/3a-VALIDATION.md` returns the current scheduler command set.
  </acceptance_criteria>
  <action>Write `3a-VERIFICATION.md` against today's repository state, not the 2026-04-03 verify block. Record one passing command for `SCHED-01..06` using `tests/test_scheduler.py`, and one passing command for `SCHED-07..08` using the refreshed integration tests from Task 1. Then write `3a-VALIDATION.md` with a per-task verification map that names those automated commands, keeps the full-suite command on `PYTHONPATH=src uv run pytest --ignore=tests/runtime/backend/test_pi_adapter.py`, and marks Wave 0 empty once the stale lifecycle tests are fixed. Call out that these files are intentionally backfilled inside `3a-scheduler/`, because audit only recognizes the original phase directory.</action>
  <verify>
    <automated>test -f .planning/phases/3a-scheduler/3a-VERIFICATION.md && test -f .planning/phases/3a-scheduler/3a-VALIDATION.md && rg -n "^status: passed$" .planning/phases/3a-scheduler/3a-VERIFICATION.md && rg -n "^nyquist_compliant: true$" .planning/phases/3a-scheduler/3a-VALIDATION.md && rg -n "tests/test_scheduler.py|tests/test_scheduler_integration.py" .planning/phases/3a-scheduler/3a-VERIFICATION.md .planning/phases/3a-scheduler/3a-VALIDATION.md</automated>
  </verify>
  <done>The original 3a directory has a complete audit-recognized artifact chain: summaries, verification, and validation built from passing current commands.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=src uv run pytest -q tests/test_scheduler.py tests/test_scheduler_integration.py` after Task 1.
Run both `summary-extract` commands after Task 2.
Check `3a-VERIFICATION.md` and `3a-VALIDATION.md` with the grep commands from Task 3 before writing the Phase 06 summary.
</verification>

<success_criteria>
`SCHED-01..08` are traceable through `3a-01/02-SUMMARY.md`, `3a-VERIFICATION.md`, and `3a-VALIDATION.md`.
`tests/test_scheduler_integration.py` proves `RuntimeApp` lifecycle integration on the current codebase without changing scheduler production behavior.
Re-running milestone audit can consume the 3a artifact chain from the original phase directory.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-infra-artifact-backfill/06-01-SUMMARY.md`
</output>
