---
phase: 04-unified-message-tool-playwright
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/acabot/types/action.py
  - src/acabot/runtime/builtin_tools/message.py
  - src/acabot/runtime/builtin_tools/__init__.py
  - src/acabot/gateway/napcat.py
  - tests/runtime/test_message_tool.py
  - tests/runtime/test_builtin_tools.py
  - tests/test_gateway.py
autonomous: true
requirements:
  - MSG-04
  - MSG-05
  - MSG-07
  - MSG-10
must_haves:
  truths:
    - "模型只能看到一个统一的 `message` builtin tool, `action` 默认是 `send`."
    - "`message.react` 和 `message.recall` 直接产出低层 Action, 不在 tool 里调用 Gateway."
    - "未知 reaction emoji 会严格失败, 不会静默降级成普通文本."
    - "`message` tool 的 agent-facing schema 文案明确写清默认回复抑制规则，并要求把 `text` / `images` / `render` 组合在同一次 `send` 调用里。"
  artifacts:
    - path: "src/acabot/runtime/builtin_tools/message.py"
      provides: "Unified message tool surface with send/react/recall handlers"
      contains: "class BuiltinMessageToolSurface"
    - path: "src/acabot/types/action.py"
      provides: "High-level send intent action type"
      contains: "SEND_MESSAGE_INTENT"
    - path: "src/acabot/gateway/napcat.py"
      provides: "NapCat reaction payload builder"
      contains: "set_msg_emoji_like"
  key_links:
    - from: "src/acabot/runtime/builtin_tools/message.py"
      to: "src/acabot/types/action.py"
      via: "PlannedAction.action.action_type"
      pattern: "ActionType.SEND_MESSAGE_INTENT|ActionType.REACTION|ActionType.RECALL"
    - from: "src/acabot/runtime/builtin_tools/__init__.py"
      to: "src/acabot/runtime/builtin_tools/message.py"
      via: "register_core_builtin_tools"
      pattern: "BuiltinMessageToolSurface"
    - from: "src/acabot/gateway/napcat.py"
      to: "tests/test_gateway.py"
      via: "build_send_payload"
      pattern: "set_msg_emoji_like"
---

<objective>
把 Phase 04 的入口面先收干净：新增统一 `message` builtin tool，锁定 send/react/recall 三种 action 的 schema，并补齐 NapCat reaction 的低层 payload。

Purpose: 先把“模型怎么表达消息意图”这个 surface 定死，后面的 Outbox materialization、cross-session 语义和 render service 才有稳定输入。
Output: 可注册的 `BuiltinMessageToolSurface`、新的高层 `SEND_MESSAGE_INTENT` action type、reaction gateway payload、以及对应的单元测试。
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
@.planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
@.planning/phases/04-unified-message-tool-playwright/04-VALIDATION.md
@src/acabot/types/action.py
@src/acabot/runtime/builtin_tools/__init__.py
@src/acabot/runtime/tool_broker/contracts.py
@src/acabot/gateway/napcat.py
@tests/runtime/test_builtin_tools.py
@tests/test_gateway.py

<interfaces>
From `src/acabot/types/action.py`:
```python
class ActionType(str, Enum):
    SEND_TEXT = "send_text"
    SEND_SEGMENTS = "send_segments"
    RECALL = "recall"
    GROUP_BAN = "group_ban"
    GROUP_KICK = "group_kick"
    TYPING = "typing"
    REACTION = "reaction"

@dataclass
class Action:
    action_type: ActionType
    target: EventSource
    payload: dict[str, Any] = field(default_factory=dict)
    reply_to: str | None = None
```

From `src/acabot/runtime/tool_broker/contracts.py`:
```python
@dataclass(slots=True)
class ToolResult:
    llm_content: str | list[dict[str, Any]] = ""
    attachments: list[Attachment] = field(default_factory=list)
    user_actions: list[PlannedAction] = field(default_factory=list)
    artifacts: list[dict[str, Any]] = field(default_factory=list)
    metadata: dict[str, Any] = field(default_factory=dict)
```

Pattern to preserve from `src/acabot/runtime/builtin_tools/computer.py`:
```python
BUILTIN_COMPUTER_TOOL_SOURCE = "builtin:computer"

class BuiltinComputerToolSurface:
    def register(self, tool_broker: ToolBroker) -> list[str]:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the unified `message` builtin tool surface and locked schema</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - src/acabot/types/action.py
    - src/acabot/runtime/tool_broker/contracts.py
    - src/acabot/runtime/builtin_tools/computer.py
    - tests/runtime/test_builtin_tools.py
  </read_first>
  <files>
    src/acabot/types/action.py
    src/acabot/runtime/builtin_tools/message.py
    tests/runtime/test_message_tool.py
  </files>
  <behavior>
    - Test 1: `message` tool schema exposes only `send` / `react` / `recall`, with `action` defaulting to `send`.
    - Test 2: `send` produces one `PlannedAction` with `ActionType.SEND_MESSAGE_INTENT`, carries locked fields `text` / `images` / `render` / `reply_to` / `at_user` / `target`, and returns through `ToolResult.user_actions` only.
    - Test 3: `react` maps known aliases or Unicode emoji to a low-level `ActionType.REACTION`; unknown emoji raises instead of falling back.
    - Test 4: `recall` maps directly to `ActionType.RECALL`.
    - Test 5: `message` tool description or send-field guidance text explicitly states D-08: content-type `send` suppresses the default assistant text reply, and `text` / `images` / `render` should be combined in one send call when they belong to the same outgoing message.
  </behavior>
  <action>新增 `src/acabot/runtime/builtin_tools/message.py`，定义 `BUILTIN_MESSAGE_TOOL_SOURCE = "builtin:message"` 和 `BuiltinMessageToolSurface`，完全按锁定决策实现 schema 与 handler。按 D-01、D-02、D-03、D-04、D-05、D-08、D-09、D-10、D-11、D-12、D-20、D-21、D-22、D-23、D-24、D-25 处理：顶层只有一个 `message` tool；`action` 枚举只能是 `send` / `react` / `recall`，默认值 `"send"`；`send` 生成 `ActionType.SEND_MESSAGE_INTENT` 的 `PlannedAction`，把 `reply_to` 放进 `Action.reply_to`，把 `text`、`images`、`render`、`at_user`、`target` 放进 `Action.payload`；`react` 生成 `ActionType.REACTION`，`recall` 生成 `ActionType.RECALL`；对 `target` 做 canonical `conversation_id` 形状校验，只接受 `qq:group:...` / `qq:user:...`；在 send action 的 metadata 固定写入 `message_action="send"`、`suppresses_default_reply=True`、`destination_conversation_id=<canonical target or current conversation>`，不要在 tool 内直接碰 Gateway、Outbox 或 MessageStore。工具的 agent-facing description / parameters 文案必须显式写出 D-08：如果 `message.action="send"` 已经发出了内容型消息，runtime 会自动抑制本轮默认 assistant 文本回复；如果想“图片 + 说明文字”或“渲染图 + 说明文字”一起发，必须把 `text` / `images` / `render` 组合在同一次 `send` 调用里。同步修改 `src/acabot/types/action.py`，新增 `ActionType.SEND_MESSAGE_INTENT = "send_message_intent"`，不要修改现有低层 action 名字。新增 `tests/runtime/test_message_tool.py`，覆盖上面的 locked schema、user_actions-only 行为、canonical target 校验、严格 reaction 失败语义，以及 D-08 的 schema guidance 文案断言。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_message_tool.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/types/action.py` 包含 `SEND_MESSAGE_INTENT = "send_message_intent"`。
    - `src/acabot/runtime/builtin_tools/message.py` 定义 `BUILTIN_MESSAGE_TOOL_SOURCE` 和 `BuiltinMessageToolSurface`。
    - `tests/runtime/test_message_tool.py` 存在，并包含 `test_message_tool_schema_matches_locked_fields`、`test_message_tool_returns_user_actions_only`、`test_message_react_requires_known_emoji`、`test_message_recall_returns_low_level_action`、`test_message_tool_schema_includes_send_guidance`。
    - `message` tool 的 agent-facing schema 文案包含“content-type send suppresses default assistant text reply”语义，以及“combine text/images/render in one send call”语义。
    - `PYTHONPATH=src uv run pytest -q tests/runtime/test_message_tool.py` 全绿。
  </acceptance_criteria>
  <done>统一 tool surface 已存在，send/react/recall 的 contract 被锁定为可测试代码，后续 plan 可以直接围绕 `SEND_MESSAGE_INTENT` 往下接。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register `message` as a core builtin tool and add NapCat reaction payload support</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - src/acabot/runtime/builtin_tools/message.py
    - src/acabot/runtime/builtin_tools/__init__.py
    - src/acabot/gateway/napcat.py
    - tests/runtime/test_builtin_tools.py
    - tests/test_gateway.py
  </read_first>
  <files>
    src/acabot/runtime/builtin_tools/__init__.py
    src/acabot/gateway/napcat.py
    tests/runtime/test_builtin_tools.py
    tests/test_gateway.py
  </files>
  <behavior>
    - Test 1: `build_runtime_components()` 注册后，ToolBroker 里存在来源为 `builtin:message` 的 `message` 工具。
    - Test 2: `NapCatGateway.build_send_payload()` 遇到 `ActionType.REACTION` 时，生成 `set_msg_emoji_like` payload，参数为 `message_id` 和 `emoji_id`。
    - Test 3: 现有 recall 路径仍然走 `delete_msg`，不回归。
  </behavior>
  <action>在 `src/acabot/runtime/builtin_tools/__init__.py` 中引入并注册 `BuiltinMessageToolSurface`，把 `message` 纳入 `register_core_builtin_tools()` 的返回字典，并把 `BUILTIN_MESSAGE_TOOL_SOURCE` 加入 `__all__`。在 `src/acabot/gateway/napcat.py` 的 `build_send_payload()` 中新增 `ActionType.REACTION` 分支，映射到 NapCat 扩展 API `set_msg_emoji_like`，参数固定使用 `{"message_id": action.payload["message_id"], "emoji_id": action.payload["emoji_id"]}`；不要让 REACTION 走 `_build_msg_payload()`，也不要改动 RECALL 的 `delete_msg` 语义。同步补 `tests/runtime/test_builtin_tools.py`，断言 `message` source 是 `builtin:message`；补 `tests/test_gateway.py`，断言 reaction payload 与 recall payload 都正确。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_builtin_tools.py tests/test_gateway.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/builtin_tools/__init__.py` 中存在 `BuiltinMessageToolSurface` 注册调用。
    - `tests/runtime/test_builtin_tools.py` 能断言 `message` 工具来源为 `builtin:message`。
    - `src/acabot/gateway/napcat.py` 中存在 `ActionType.REACTION` 分支和 `set_msg_emoji_like`。
    - `tests/test_gateway.py` 包含 reaction payload 测试和 recall 不回归测试。
    - `PYTHONPATH=src uv run pytest -q tests/runtime/test_builtin_tools.py tests/test_gateway.py` 全绿。
  </acceptance_criteria>
  <done>core builtin tool 目录里正式出现 `message`，NapCat 低层 reaction payload 可发送，Plan 02 可以专注处理 send intent materialization 和线程语义。</done>
</task>

</tasks>

<verification>
运行 `PYTHONPATH=src uv run pytest -q tests/runtime/test_message_tool.py tests/runtime/test_builtin_tools.py tests/test_gateway.py`，确认统一 tool surface、builtin 注册链和 NapCat reaction payload 都通过。
</verification>

<success_criteria>
1. ToolBroker 注册结果中可见 `message`，来源为 `builtin:message`。
2. `message.send` 只产出高层 `SEND_MESSAGE_INTENT`，`message.react` / `message.recall` 只产出低层 `REACTION` / `RECALL`。
3. `REACTION` 的 NapCat payload 通过单测固定为 `set_msg_emoji_like(message_id, emoji_id)`。
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-message-tool-playwright/04-01-SUMMARY.md`
</output>
