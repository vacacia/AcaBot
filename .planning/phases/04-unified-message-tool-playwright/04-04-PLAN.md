---
phase: 04-unified-message-tool-playwright
plan: 4
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - src/acabot/runtime/outbox.py
  - src/acabot/runtime/bootstrap/__init__.py
  - src/acabot/runtime/bootstrap/components.py
  - src/acabot/runtime/app.py
  - tests/runtime/test_outbox.py
  - tests/runtime/test_bootstrap.py
  - tests/runtime/test_app.py
  - docs/01-system-map.md
  - docs/03-data-contracts.md
  - docs/07-gateway-and-channel-layer.md
  - docs/12-computer.md
autonomous: true
requirements:
  - MSG-08
  - PW-01
must_haves:
  truths:
    - "default bootstrap-built RenderService 会显式注册 `PlaywrightRenderBackend`，并把同一个 service 实例注入 Outbox。"
    - "Outbox 通过注入的 render service 解析 `render` 字段，而不是自己 new backend 或依赖模块级单例。"
    - "RuntimeApp.stop() 会关闭 render service，确保 render backend 生命周期被统一回收。"
  artifacts:
    - path: "src/acabot/runtime/bootstrap/__init__.py"
      provides: "Default render service construction and backend registration"
      contains: "PlaywrightRenderBackend"
    - path: "src/acabot/runtime/outbox.py"
      provides: "render-aware send materialization using injected service"
      contains: "render_markdown_to_image"
    - path: "src/acabot/runtime/app.py"
      provides: "Render service shutdown hook"
      contains: "render_service"
  key_links:
    - from: "src/acabot/runtime/bootstrap/__init__.py"
      to: "src/acabot/runtime/render/playwright_backend.py"
      via: "bootstrap explicitly registers `PlaywrightRenderBackend` into the default `RenderService`"
      pattern: "PlaywrightRenderBackend"
    - from: "src/acabot/runtime/bootstrap/__init__.py"
      to: "src/acabot/runtime/outbox.py"
      via: "the same `render_service` instance is passed into `Outbox(...)`"
      pattern: "render_service="
    - from: "src/acabot/runtime/outbox.py"
      to: "src/acabot/runtime/render/service.py"
      via: "Outbox resolves render through the injected service"
      pattern: "render_markdown_to_image"
    - from: "src/acabot/runtime/app.py"
      to: "src/acabot/runtime/render/service.py"
      via: "RuntimeApp.stop() calls `close()` on the render service"
      pattern: "close"
---

<objective>
把 04-03 做好的 render module 真正接上线：bootstrap 显式注册默认 `PlaywrightRenderBackend`，Outbox 使用注入的 service，RuntimeApp 负责关闭，文档和回归测试一起补齐。

Purpose: 证明 default render path 不是“有模块但没人接”，而是从 bootstrap 到 Outbox 到 app shutdown 都有清楚的 wiring 证据链。
Output: render-aware Outbox、默认 render service wiring、shutdown hook、更新后的 docs 与 regression tests。
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
@.planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
@.planning/phases/04-unified-message-tool-playwright/04-VALIDATION.md
@src/acabot/runtime/render/service.py
@src/acabot/runtime/render/playwright_backend.py
@src/acabot/runtime/outbox.py
@src/acabot/runtime/bootstrap/__init__.py
@src/acabot/runtime/bootstrap/components.py
@src/acabot/runtime/app.py
@tests/runtime/test_outbox.py
@tests/runtime/test_bootstrap.py
@tests/runtime/test_app.py
@docs/01-system-map.md
@docs/03-data-contracts.md
@docs/07-gateway-and-channel-layer.md
@docs/12-computer.md

<interfaces>
Current Outbox constructor from `src/acabot/runtime/outbox.py`:
```python
class Outbox:
    def __init__(
        self,
        *,
        gateway: GatewayProtocol,
        store: MessageStore,
        long_term_memory_ingestor: LongTermMemoryIngestor | None = None,
    ) -> None:
        ...
```

Current bootstrap wiring from `src/acabot/runtime/bootstrap/__init__.py`:
```python
outbox = Outbox(
    gateway=gateway,
    store=runtime_message_store,
    long_term_memory_ingestor=runtime_long_term_memory_ingestor,
)
```

Current shutdown site from `src/acabot/runtime/app.py`:
```python
async def stop(self) -> None:
    await self.gateway.stop()
    if self.scheduler is not None:
        await self.scheduler.stop()
    if self.plugin_runtime_host is not None:
        await self.plugin_runtime_host.teardown_all()
    if self.long_term_memory_ingestor is not None:
        await self.long_term_memory_ingestor.stop()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Explicitly register `PlaywrightRenderBackend` into the default bootstrap render service and inject it into Outbox</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - src/acabot/runtime/render/service.py
    - src/acabot/runtime/render/playwright_backend.py
    - src/acabot/runtime/outbox.py
    - src/acabot/runtime/bootstrap/__init__.py
    - src/acabot/runtime/bootstrap/components.py
    - tests/runtime/test_outbox.py
    - tests/runtime/test_bootstrap.py
  </read_first>
  <files>
    src/acabot/runtime/outbox.py
    src/acabot/runtime/bootstrap/__init__.py
    src/acabot/runtime/bootstrap/components.py
    tests/runtime/test_outbox.py
    tests/runtime/test_bootstrap.py
  </files>
  <behavior>
    - Test 1: bootstrap creates one default `RenderService`, instantiates `PlaywrightRenderBackend`, and explicitly registers that backend into the service.
    - Test 2: bootstrap passes the same `RenderService` instance into `Outbox`.
    - Test 3: `Outbox` resolves `render` through the injected service by calling `render_markdown_to_image()`, not by constructing a backend itself.
    - Test 4: when the injected service reports unavailable or render failure, Outbox falls back to raw text.
  </behavior>
  <action>把 render service 接进主线，而且 wiring 必须写得可证。修改 `src/acabot/runtime/bootstrap/__init__.py`：构造一个默认 `RenderService` 实例，实例化 `PlaywrightRenderBackend`，然后显式调用 registry API 把 backend 注册进这个默认 service，例如 `runtime_render_service.register_backend("playwright", runtime_playwright_backend, is_default=True)` 或等价明确写法，不要依赖 service 自己偷偷隐式发现 backend。随后把同一个 `runtime_render_service` 传给 `Outbox(...)`。同步在 `src/acabot/runtime/bootstrap/components.py` 的 `RuntimeComponents` 中新增 `render_service` 字段，方便测试直接证明 bootstrap 的 service 就是 Outbox 拿到的那一个。修改 `src/acabot/runtime/outbox.py`：给 `Outbox.__init__()` 新增 `render_service` 依赖，在 `SEND_MESSAGE_INTENT` materialization 里通过注入的 service 调用 `render_markdown_to_image()`；成功时把渲染产物编成 `image` segment，失败或 backend 不可用时按 D-19 回退为原始 markdown 文本，不要在 Outbox 里自己 new `PlaywrightRenderBackend`。补 `tests/runtime/test_bootstrap.py` 与 `tests/runtime/test_outbox.py`，至少包含 `test_build_runtime_components_registers_playwright_backend_in_default_render_service`、`test_build_runtime_components_injects_same_render_service_into_outbox`、`test_outbox_calls_injected_render_service_in_materialization` 这类断言。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py tests/runtime/test_bootstrap.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/bootstrap/__init__.py` 中存在显式 `PlaywrightRenderBackend` 实例化和默认 render service 注册代码。
    - `src/acabot/runtime/bootstrap/__init__.py` 中 `Outbox(...)` 接收到 `render_service=` 参数。
    - `src/acabot/runtime/bootstrap/components.py` 暴露 `render_service` 字段。
    - `src/acabot/runtime/outbox.py` 中存在对注入 `render_service.render_markdown_to_image()` 的调用，而不是直接构造 backend。
    - `tests/runtime/test_bootstrap.py` 证明默认 service 注册了 `PlaywrightRenderBackend`，且 Outbox 拿到的是同一实例。
    - `tests/runtime/test_outbox.py` 证明 Outbox 调用的是注入 service，并保留 raw-text fallback。
    - `PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py tests/runtime/test_bootstrap.py` 全绿。
  </acceptance_criteria>
  <done>default render path 的 wiring 已经可证：bootstrap 显式注册 backend，Outbox 用注入 service，render 不再是“模块存在但没人接”的悬空能力。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the render service during shutdown and document the new runtime boundary</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - src/acabot/runtime/app.py
    - src/acabot/runtime/outbox.py
    - docs/01-system-map.md
    - docs/03-data-contracts.md
    - docs/07-gateway-and-channel-layer.md
    - docs/12-computer.md
    - tests/runtime/test_app.py
  </read_first>
  <files>
    src/acabot/runtime/app.py
    tests/runtime/test_app.py
    docs/01-system-map.md
    docs/03-data-contracts.md
    docs/07-gateway-and-channel-layer.md
    docs/12-computer.md
  </files>
  <behavior>
    - Test 1: `RuntimeApp.stop()` closes the render service exactly once during shutdown.
    - Test 2: docs describe `message` tool → `SEND_MESSAGE_INTENT` → Outbox materialization → Gateway, and keep render out of Gateway / Work World.
  </behavior>
  <action>修改 `src/acabot/runtime/app.py`，在 `stop()` 链路末尾调用 render service 的 `close()`，保证 PW-02 的 browser lifecycle 真正收尾；如果关闭 render service 失败，沿用现有 shutdown 错误聚合模式，不要吞错。同步更新 4 份正式文档，全部按“现在在做什么”的口径：`docs/01-system-map.md` 明确 `message` tool → `SEND_MESSAGE_INTENT` → `Outbox materialization` → `Gateway`；`docs/03-data-contracts.md` 新增 `SEND_MESSAGE_INTENT`、扩展 `OutboxItem` destination fields、说明 cross-session send 的 destination persistence 语义；`docs/07-gateway-and-channel-layer.md` 写清 reaction 仍是 NapCat-specific low-level action，而 render 不属于 gateway；`docs/12-computer.md` 明确 render artifacts 不进 `/workspace/attachments`，而是 internal runtime artifacts。不要写 Deferred Ideas，不要重新讨论 schema 选择。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_app.py tests/runtime/test_outbox.py tests/runtime/test_bootstrap.py tests/runtime/test_model_agent_runtime.py tests/runtime/test_pipeline_runtime.py tests/test_gateway.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/app.py` 的 `stop()` 会关闭 render service。
    - `tests/runtime/test_app.py` 包含 render service shutdown 测试。
    - `docs/01-system-map.md` 明确出现 `SEND_MESSAGE_INTENT` 和 Outbox materialization。
    - `docs/03-data-contracts.md` 明确出现 `destination_thread_id` 与 `destination_conversation_id` 的区别。
    - `docs/07-gateway-and-channel-layer.md` 明确写出 render 不下沉到 gateway、reaction 仍是低层 NapCat action。
    - `docs/12-computer.md` 明确写出 render artifacts 不进 Work World attachments。
    - 指定回归测试命令通过。
  </acceptance_criteria>
  <done>render lifecycle、主线边界和文档都对齐了，Phase 04 的 render path 从实现到解释都闭合。</done>
</task>

</tasks>

<verification>
先跑 `PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py tests/runtime/test_bootstrap.py`，再跑 `PYTHONPATH=src uv run pytest -q tests/runtime/test_app.py tests/runtime/test_model_agent_runtime.py tests/runtime/test_pipeline_runtime.py tests/test_gateway.py` 做主链回归。
</verification>

<success_criteria>
1. default bootstrap-built RenderService 显式注册 `PlaywrightRenderBackend`，并把同一实例注入 Outbox。
2. Outbox 通过注入的 service 解析 `render`，成功发图，失败回退 raw markdown 文本。
3. RuntimeApp.stop() 会关闭 render service，文档同步反映新的 message/render 边界。
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-message-tool-playwright/04-04-SUMMARY.md`
</output>
