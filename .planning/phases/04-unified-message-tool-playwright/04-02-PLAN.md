---
phase: 04-unified-message-tool-playwright
plan: 2
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/acabot/runtime/ids.py
  - src/acabot/runtime/contracts/context.py
  - src/acabot/runtime/outbox.py
  - src/acabot/runtime/model/model_agent_runtime.py
  - src/acabot/runtime/pipeline.py
  - tests/runtime/test_outbox.py
  - tests/runtime/test_model_agent_runtime.py
  - tests/runtime/test_pipeline_runtime.py
autonomous: true
requirements:
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-06
  - MSG-09
must_haves:
  truths:
    - "`message.send` 会被物化成一条可发送的低层消息, 同时支持 quote、@、text、images 和跨会话 target。"
    - "cross-session send 的事实记录和 thread working memory 写到目标会话，不会误写回来源 thread。"
    - "只有内容型 `message.send` 会抑制默认 assistant 文本回复，`react` / `recall` 不会。"
    - "`conversation_id` 表示外部对话容器，`thread_id` 表示 runtime 内部执行线程；cross-session send 会同时保留这两个字段，即使 v1 的 helper 暂时让它们值相同。"
  artifacts:
    - path: "src/acabot/runtime/ids.py"
      provides: "Shared canonical conversation_id parsing/building helpers"
      contains: "parse_conversation_id"
    - path: "src/acabot/runtime/outbox.py"
      provides: "SEND_MESSAGE_INTENT materialization and destination-thread persistence"
      contains: "ActionType.SEND_MESSAGE_INTENT"
    - path: "src/acabot/runtime/model/model_agent_runtime.py"
      provides: "Default reply suppression at the append-text site"
      contains: "suppresses_default_reply"
    - path: "src/acabot/runtime/pipeline.py"
      provides: "Destination-thread working memory update logic"
      contains: "destination_thread_id"
  key_links:
    - from: "src/acabot/runtime/builtin_tools/message.py"
      to: "src/acabot/runtime/model/model_agent_runtime.py"
      via: "PlannedAction.metadata['suppresses_default_reply']"
      pattern: "suppresses_default_reply"
    - from: "src/acabot/runtime/outbox.py"
      to: "src/acabot/gateway/napcat.py"
      via: "materialized SEND_SEGMENTS action"
      pattern: "ActionType.SEND_SEGMENTS"
    - from: "src/acabot/runtime/outbox.py"
      to: "src/acabot/runtime/pipeline.py"
      via: "OutboxItem.destination_thread_id"
      pattern: "destination_thread_id"
---

<objective>
把 `message.send` 这条高层 intent 真的接进 runtime 主线：Outbox 负责物化，pipeline 负责正确更新目标 thread，`ModelAgentRuntime` 负责默认回复抑制。

Purpose: 让 Phase 04 的 send 路径真正可发、可落库、可跨会话，不把事实和 working memory 写错地方。
Output: canonical conversation helper、扩展后的 OutboxItem / materialization、default reply suppression，以及 cross-session 相关测试。
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
@.planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
@.planning/phases/04-unified-message-tool-playwright/04-VALIDATION.md
@src/acabot/runtime/contracts/context.py
@src/acabot/runtime/outbox.py
@src/acabot/runtime/model/model_agent_runtime.py
@src/acabot/runtime/pipeline.py
@src/acabot/runtime/router.py
@src/acabot/runtime/control/session_runtime.py
@tests/runtime/test_outbox.py
@tests/runtime/test_model_agent_runtime.py
@tests/runtime/test_pipeline_runtime.py

<interfaces>
From `src/acabot/runtime/contracts/context.py`:
```python
@dataclass(slots=True)
class PlannedAction:
    action_id: str
    action: Action
    thread_content: str | None = None
    commit_when: CommitWhen = "success"
    metadata: dict[str, Any] = field(default_factory=dict)

@dataclass(slots=True)
class OutboxItem:
    thread_id: str
    run_id: str
    agent_id: str
    plan: PlannedAction
    metadata: dict[str, Any] = field(default_factory=dict)
```

From `src/acabot/runtime/outbox.py`:
```python
def _build_items(self, ctx: RunContext) -> list[OutboxItem]:
    return [
        OutboxItem(
            thread_id=ctx.thread.thread_id,
            run_id=ctx.run.run_id,
            agent_id=ctx.agent.agent_id,
            plan=plan,
            metadata={"channel_scope": ctx.thread.channel_scope},
        )
        for plan in ctx.actions
    ]
```

From `src/acabot/runtime/model/model_agent_runtime.py`:
```python
text = str(getattr(response, "text", "") or "")
actions = list(tool_runtime.state.user_actions)
if text:
    actions.append(self._build_text_reply_action(ctx, text))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical destination-thread contracts for send intent delivery</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - docs/00-ai-entry.md
    - docs/03-data-contracts.md
    - src/acabot/runtime/contracts/context.py
    - src/acabot/runtime/router.py
    - src/acabot/runtime/control/session_runtime.py
    - tests/runtime/test_outbox.py
    - tests/runtime/test_pipeline_runtime.py
  </read_first>
  <files>
    src/acabot/runtime/ids.py
    src/acabot/runtime/contracts/context.py
    tests/runtime/test_outbox.py
    tests/runtime/test_pipeline_runtime.py
  </files>
  <behavior>
    - Test 1: canonical helper accepts only `qq:group:<id>` / `qq:user:<id>` and returns a destination `EventSource`.
    - Test 2: `OutboxItem` carries origin thread vs destination thread separately.
    - Test 3: cross-session send persists facts under destination thread id instead of origin thread id.
  </behavior>
  <action>新增 `src/acabot/runtime/ids.py`，集中放 `conversation_id` helper，不再在新代码里手写 split 逻辑。这里必须按 `docs/00-ai-entry.md` 和 `docs/03-data-contracts.md` 的正式词典明示 contract：`conversation_id` 是外部消息容器 ID，`thread_id` 是 runtime 内部执行线程 ID，二者语义不同；v1 可以让 `build_thread_id_from_conversation_id()` 暂时返回同一个 canonical 字符串，但代码字段和测试断言必须继续把它们分开保留，不能把 `conversation_id` 当成 `thread_id` 的别名来传。实现三个公开函数：`parse_conversation_id(conversation_id: str) -> tuple[str, str]`，严格只接受 D-20、D-21 允许的 canonical 形式；`build_event_source_from_conversation_id(conversation_id: str, *, actor_user_id: str) -> EventSource`，把目标会话转成可发送的 `EventSource`；`build_thread_id_from_conversation_id(conversation_id: str) -> str`，统一生成目标 runtime thread id。同步扩展 `src/acabot/runtime/contracts/context.py` 的 `OutboxItem`，新增 `origin_thread_id`、`destination_thread_id`、`destination_conversation_id`、`append_to_origin_thread` 字段，保留现有 `plan` 和 `metadata`。补 `tests/runtime/test_outbox.py` 与 `tests/runtime/test_pipeline_runtime.py`，先把新的 destination contract 固定下来，避免后续继续默认使用 `ctx.thread.thread_id`，也避免把 cross-session target 错记成 origin thread。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py tests/runtime/test_pipeline_runtime.py -k "cross_session or destination_thread or conversation_id"</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/ids.py` 存在，并导出 `parse_conversation_id`、`build_event_source_from_conversation_id`、`build_thread_id_from_conversation_id`。
    - `src/acabot/runtime/contracts/context.py` 的 `OutboxItem` 包含 `origin_thread_id`、`destination_thread_id`、`destination_conversation_id`、`append_to_origin_thread`。
    - 任务产出的 helper / dataclass / tests 明确保留 `destination_conversation_id` 和 `destination_thread_id` 两套字段，并把前者定义为外部容器 ID、后者定义为 runtime 执行线程 ID。
    - `tests/runtime/test_outbox.py` 和 `tests/runtime/test_pipeline_runtime.py` 中存在 cross-session destination contract 测试。
    - 指定的 pytest 子集通过。
  </acceptance_criteria>
  <done>cross-session send 的 destination contract 被固定成明确字段，后续 Outbox / Pipeline 不再靠隐式 metadata 猜线程。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize `SEND_MESSAGE_INTENT` into one low-level send action</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - src/acabot/runtime/builtin_tools/message.py
    - src/acabot/runtime/contracts/context.py
    - src/acabot/runtime/ids.py
    - src/acabot/runtime/outbox.py
    - tests/runtime/test_outbox.py
  </read_first>
  <files>
    src/acabot/runtime/outbox.py
    tests/runtime/test_outbox.py
  </files>
  <behavior>
    - Test 1: `SEND_MESSAGE_INTENT` with `text` and `reply_to` materializes into one `SEND_SEGMENTS` action preserving `Action.reply_to`.
    - Test 2: `at_user` becomes an `at` segment before text.
    - Test 3: `images` become `image` segments in the same low-level action.
    - Test 4: when no render backend exists yet, `render` degrades to a plain text segment with raw source, not a crash.
    - Test 5: `target` rewrites destination thread / persistence to the target conversation only.
  </behavior>
  <action>在 `src/acabot/runtime/outbox.py` 中新增 `ActionType.SEND_MESSAGE_INTENT` 的 materialization 分支，按 D-03、D-05、D-13、D-19、D-20、D-21、D-23、D-24 把高层 send intent 统一编译成一条低层 `ActionType.SEND_SEGMENTS`。低层 segment 顺序固定为 `at -> text -> images -> render-fallback-text`；`reply_to` 继续放在 `Action.reply_to`，不要转进 payload；`target` 通过 `src/acabot/runtime/ids.py` 转成目标 `EventSource` 和 `destination_thread_id`；本地图片路径和远程 URL 都在这里转成 `{"type": "image", "data": {"file": ...}}` 段，暂时不要引入 render service；`render` 在本 plan 里一律退化成普通 text segment，为 Plan 03 留出无缝升级点。同步改 `_build_items()`、`_persist_success()` 和 LTM `mark_dirty()`，全部使用 `destination_thread_id` / `destination_conversation_id`，绝对不要继续默认写入 `ctx.thread.thread_id`。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/outbox.py` 中显式处理 `ActionType.SEND_MESSAGE_INTENT`。
    - `_build_items()` 生成的 `OutboxItem` 使用 destination fields，不再只保存当前 thread。
    - `_persist_success()` 和 `mark_dirty()` 使用 `destination_thread_id`。
    - `tests/runtime/test_outbox.py` 包含 `test_outbox_materializes_images`、`test_outbox_builds_at_segment_before_text`、`test_outbox_persists_cross_session_delivery_to_destination` 这类覆盖 send intent 的测试。
    - `PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py` 全绿。
  </acceptance_criteria>
  <done>高层 `message.send` 已经真正接入 `Action -> Outbox -> Gateway` 主线，且 send intent 的 destination persistence 语义不再含糊。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suppress duplicate default replies and update the correct thread after delivery</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - src/acabot/runtime/model/model_agent_runtime.py
    - src/acabot/runtime/pipeline.py
    - src/acabot/runtime/contracts/context.py
    - tests/runtime/test_model_agent_runtime.py
    - tests/runtime/test_pipeline_runtime.py
  </read_first>
  <files>
    src/acabot/runtime/model/model_agent_runtime.py
    src/acabot/runtime/pipeline.py
    tests/runtime/test_model_agent_runtime.py
    tests/runtime/test_pipeline_runtime.py
  </files>
  <behavior>
    - Test 1: when a completed run already contains a content-type `message.send`, `ModelAgentRuntime` does not append the default assistant text action.
    - Test 2: `react` / `recall` do not suppress the default assistant text action.
    - Test 3: cross-session delivered content updates the destination thread working memory, not the origin thread.
  </behavior>
  <action>在 `src/acabot/runtime/model/model_agent_runtime.py` 的 `_to_runtime_result()` 里实现默认回复抑制，这是 research 指定的唯一正确位置。按 D-06、D-07、D-08 读取 `tool_runtime.state.user_actions` 上的 action metadata：只要存在 `metadata["suppresses_default_reply"] is True` 且 `action.action_type == ActionType.SEND_MESSAGE_INTENT` 的内容型消息，就不要再追加 `_build_text_reply_action()`；`react` / `recall` 一律不能触发这个分支。然后在 `src/acabot/runtime/pipeline.py` 的 `_update_thread_after_send()` 里停止“所有送达内容都写回当前 thread”这个旧假设：当 `destination_thread_id == ctx.thread.thread_id` 时沿用当前线程；当目标线程不同，调用 `thread_manager.get_or_create(destination_thread_id, channel_scope=destination_conversation_id, last_event_at=ctx.event.timestamp)`，把 assistant `thread_content` 写到目标 thread，再单独保存；来源 thread 只保留用户输入，不追加跨会话回复。不要把任何 cross-session send 内容写到 origin thread metadata、working_messages 或 LTM dirty 集合里。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_model_agent_runtime.py tests/runtime/test_pipeline_runtime.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/model/model_agent_runtime.py` 中，默认文本追加逻辑前有基于 action metadata 的 suppression helper。
    - suppression 只识别内容型 `SEND_MESSAGE_INTENT`，不靠 tool name 字符串匹配。
    - `src/acabot/runtime/pipeline.py` 中根据 `destination_thread_id` 分流 thread 更新。
    - `tests/runtime/test_model_agent_runtime.py` 包含默认回复抑制与非抑制测试。
    - `tests/runtime/test_pipeline_runtime.py` 包含 cross-session destination thread working memory 测试。
    - 指定 pytest 子集通过。
  </acceptance_criteria>
  <done>send intent 的行为链已经闭合：同一条消息不会被“tool send + assistant text”重复发送，cross-session 也不会把事实和 working memory 写错位置。</done>
</task>

</tasks>

<verification>
运行 `PYTHONPATH=src uv run pytest -q tests/runtime/test_outbox.py tests/runtime/test_model_agent_runtime.py tests/runtime/test_pipeline_runtime.py`，确认 send intent、cross-session 语义和默认回复抑制全部打通。
</verification>

<success_criteria>
1. `message.send` 被物化成一条低层消息，同时支持 `reply_to`、`at_user`、`images`、`target`。
2. cross-session send 的消息事实、thread working memory、LTM dirty 标记全部落在目标 thread。
3. 内容型 `message.send` 发出后，本轮默认 assistant 文本不会再重复补发。
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-message-tool-playwright/04-02-SUMMARY.md`
</output>
