---
phase: 04-unified-message-tool-playwright
plan: 3
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - pyproject.toml
  - uv.lock
  - Dockerfile
  - Dockerfile.lite
  - src/acabot/runtime/render/__init__.py
  - src/acabot/runtime/render/protocol.py
  - src/acabot/runtime/render/service.py
  - src/acabot/runtime/render/artifacts.py
  - src/acabot/runtime/render/playwright_backend.py
  - tests/runtime/test_render_service.py
autonomous: true
requirements:
  - PW-02
  - PW-03
must_haves:
  truths:
    - "render capability 是 runtime 的 optional 子模块；即使没有可用 backend，service 也能安全返回 unavailable，而不是拖垮启动。"
    - "Playwright browser 实例按需启动且复用，不会每次 render 都新开一个 browser。"
    - "render artifacts 存在 runtime 内部目录，不会混进 `/workspace/attachments` 或 Work World。"
  artifacts:
    - path: "src/acabot/runtime/render/service.py"
      provides: "Capability-based render service with backend registry API"
      contains: "class RenderService"
    - path: "src/acabot/runtime/render/playwright_backend.py"
      provides: "Lazy singleton Playwright backend"
      contains: "class PlaywrightRenderBackend"
    - path: "src/acabot/runtime/render/artifacts.py"
      provides: "Internal render artifact path helpers"
      contains: "render_artifacts"
    - path: "pyproject.toml"
      provides: "Project-level render dependencies"
      contains: "playwright"
  key_links:
    - from: "src/acabot/runtime/render/protocol.py"
      to: "src/acabot/runtime/render/playwright_backend.py"
      via: "PlaywrightRenderBackend implements the RenderBackend contract"
      pattern: "class PlaywrightRenderBackend"
    - from: "src/acabot/runtime/render/service.py"
      to: "src/acabot/runtime/render/artifacts.py"
      via: "render service allocates internal artifact paths through helper functions"
      pattern: "render_artifacts"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "render dependencies are tracked as project deps, not Dockerfile-only packages"
      pattern: "playwright|markdown-it-py|mdit-py-plugins|latex2mathml"
---

<objective>
先把 render 子模块本身做扎实：项目级依赖、render service 抽象、internal artifacts helper、Playwright backend 和它自己的单元测试。

Purpose: 把 render 的底座独立建好，再让后一份计划只负责 wiring、lifecycle 和 docs，不把 20+ 文件塞进一个 plan 里。
Output: `runtime/render/` 子模块、正式依赖声明、render backend 单测。
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
@.planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
@.planning/phases/04-unified-message-tool-playwright/04-VALIDATION.md
@pyproject.toml
@Dockerfile
@Dockerfile.lite
@src/acabot/runtime/computer/attachments.py
@src/acabot/runtime/computer/workspace.py
@docs/12-computer.md

<interfaces>
Current dependency state from `pyproject.toml`:
```toml
dependencies = [
    "websockets>=12.0",
    "litellm>=1.40.0",
    "aiosqlite>=0.20.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
    "lancedb>=0.25.0",
    "pyarrow>=18.0.0",
    "croniter>=2.0.0",
    "structlog>=24.1.0",
]
```

Current Docker image behavior from `Dockerfile` / `Dockerfile.lite`:
```dockerfile
RUN pip install playwright && playwright install --with-deps chromium
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move render dependencies into the real project dependency graph</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - pyproject.toml
    - uv.lock
    - Dockerfile
    - Dockerfile.lite
  </read_first>
  <files>
    pyproject.toml
    uv.lock
    Dockerfile
    Dockerfile.lite
  </files>
  <behavior>
    - Test 1: project dependency manifests include `playwright`、`markdown-it-py`、`mdit-py-plugins`、`latex2mathml`.
    - Test 2: Docker images stop using ad-hoc `pip install playwright` and instead only perform browser installation after project deps are installed.
  </behavior>
  <action>把 render 相关依赖变成真正的项目依赖，而不是 Dockerfile-only 临时包。修改 `pyproject.toml`，显式加入 `playwright`、`markdown-it-py`、`mdit-py-plugins`、`latex2mathml`，并更新 `uv.lock`。调整 `Dockerfile` 与 `Dockerfile.lite`：不要再 `pip install playwright`，而是依赖项目安装后的包，再执行 `python -m playwright install --with-deps chromium`。这一步只处理依赖图，不接 bootstrap / Outbox wiring。</action>
  <verify>
    <automated>rg -n 'playwright|markdown-it-py|mdit-py-plugins|latex2mathml' pyproject.toml uv.lock Dockerfile Dockerfile.lite</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` 显式包含 `playwright`、`markdown-it-py`、`mdit-py-plugins`、`latex2mathml`。
    - `uv.lock` 更新后包含上述依赖。
    - `Dockerfile` 与 `Dockerfile.lite` 不再包含 `pip install playwright`。
    - `Dockerfile` 与 `Dockerfile.lite` 改为执行 `python -m playwright install --with-deps chromium`。
  </acceptance_criteria>
  <done>render 依赖已经进入项目主依赖图，本地 / CI / 容器不会再靠 Dockerfile-only 漂移依赖碰运气。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the render module, internal artifact helpers, and Playwright backend tests</name>
  <read_first>
    - .planning/phases/04-unified-message-tool-playwright/04-CONTEXT.md
    - .planning/phases/04-unified-message-tool-playwright/04-RESEARCH.md
    - docs/12-computer.md
    - src/acabot/runtime/computer/attachments.py
    - src/acabot/runtime/computer/workspace.py
  </read_first>
  <files>
    src/acabot/runtime/render/__init__.py
    src/acabot/runtime/render/protocol.py
    src/acabot/runtime/render/service.py
    src/acabot/runtime/render/artifacts.py
    src/acabot/runtime/render/playwright_backend.py
    tests/runtime/test_render_service.py
  </files>
  <behavior>
    - Test 1: render service can return “backend unavailable” without crashing runtime startup.
    - Test 2: Playwright backend lazy-inits browser only on first render call and reuses it afterwards.
    - Test 3: markdown + inline math + block math render pipeline produces HTML payload for screenshoting.
    - Test 4: render artifacts are allocated under internal runtime paths, not `/workspace/attachments`.
  </behavior>
  <action>新增 `src/acabot/runtime/render/` 子模块，按 D-14、D-15、D-16、D-17、D-18 定义清晰边界：`protocol.py` 放 `RenderBackend` / `RenderResult` 协议；`service.py` 放 capability-based `RenderService`，包含显式 backend registry API，后续 bootstrap 会调用它注册默认 backend；`artifacts.py` 负责 internal runtime artifact 路径，固定落到 runtime 内部目录，例如 `runtime_data/render_artifacts/<conversation_id>/<run_id>/...`，绝对不要复用 `/workspace/attachments`；`playwright_backend.py` 负责 `markdown-it-py + mdit-py-plugins.dollarmath + latex2mathml -> HTML -> Playwright screenshot` 流水线，并把 browser / playwright 对象缓存为 backend 级单例。新增 `tests/runtime/test_render_service.py`，全部用 fake browser / fake playwright stub，禁止单测强依赖真实 Chromium。这里先把 module contract 和 backend 自己建好，不在本 plan 里接 bootstrap / Outbox。</action>
  <verify>
    <automated>PYTHONPATH=src uv run pytest -q tests/runtime/test_render_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/acabot/runtime/render/` 下存在 `__init__.py`、`protocol.py`、`service.py`、`artifacts.py`、`playwright_backend.py`。
    - `src/acabot/runtime/render/service.py` 提供显式 backend registry API，供后续 bootstrap 注册默认 backend。
    - `src/acabot/runtime/render/playwright_backend.py` 定义 `PlaywrightRenderBackend`，并实现 lazy browser reuse。
    - `src/acabot/runtime/render/artifacts.py` 的路径命名明确落在 internal runtime artifacts，而不是 Work World attachments。
    - `tests/runtime/test_render_service.py` 覆盖 unavailable、lazy init、singleton reuse、markdown-math pipeline。
    - `PYTHONPATH=src uv run pytest -q tests/runtime/test_render_service.py` 全绿。
  </acceptance_criteria>
  <done>render module 已经独立可测，后面的 wiring plan 只需要把这个 service 和 backend 正式接到 runtime 主线上。</done>
</task>

</tasks>

<verification>
运行 `PYTHONPATH=src uv run pytest -q tests/runtime/test_render_service.py`，并用 `rg -n 'playwright|markdown-it-py|mdit-py-plugins|latex2mathml' pyproject.toml uv.lock Dockerfile Dockerfile.lite` 确认依赖和镜像安装链一致。
</verification>

<success_criteria>
1. render 模块作为 optional capability 独立存在，包含 service、protocol、artifacts helper、Playwright backend。
2. Playwright backend 的 browser lifecycle 在单测里被证明是 lazy-init + singleton reuse。
3. render 依赖进入 `pyproject.toml` / `uv.lock`，不再是 Dockerfile-only 漂移依赖。
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-message-tool-playwright/04-03-SUMMARY.md`
</output>
