---
phase: 05-foundation-artifact-backfill
plan: 4
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-03
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md
  - .planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - PLUG-01
  - PLUG-02
  - PLUG-03
  - PLUG-04
  - PLUG-05
  - PLUG-06
  - PLUG-07
  - PLUG-08
  - PLUG-09
  - PLUG-10
  - PLUG-11
  - PLUG-12
  - PLUG-13
must_haves:
  truths:
    - "顶层 REQUIREMENTS 和 STATE 已经承认 16 个 foundation requirement 都被 Phase 05 收口，不再把 REF / PLUG gap 当成待补洞。"
    - "重新运行真实 milestone audit 流程后，REF-01..03 和 PLUG-01..13 在 audit 报告里都不再是 orphaned、partial、unsatisfied。"
    - "Phase 05 自己同时有 verification 和 validation 收口文件，清楚写出这次 backfill 关掉了哪些 foundation gap，且 `05-VALIDATION.md` 不再停在 draft。"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "foundation requirement traceability 状态更新"
      contains: "PLUG-13 | 5 | Validated"
    - path: ".planning/STATE.md"
      provides: "milestone state 与 blocker 更新"
      contains: "Phase 05"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "由真实 milestone audit workflow 重新生成的 audit 结果"
      contains: "completed_by_plans"
    - path: ".planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md"
      provides: "Phase 05 自身的验证报告"
      contains: "PLUG-13"
    - path: ".planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md"
      provides: "Phase 05 的 Nyquist 收口记录"
      contains: "## Validation Audit"
  key_links:
    - from: ".planning/phases/01-reference-backend-removal/01-VERIFICATION.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "$gsd-audit-milestone v1.0 tri-source aggregation"
      pattern: "REF-01|REF-02|REF-03"
    - from: ".planning/phases/02-plugin-reconciler/02-VERIFICATION.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "$gsd-audit-milestone v1.0 tri-source aggregation"
      pattern: "PLUG-01|PLUG-02|PLUG-03|PLUG-04|PLUG-05|PLUG-06|PLUG-07|PLUG-08|PLUG-09|PLUG-10|PLUG-11|PLUG-12|PLUG-13"
    - from: ".planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md"
      to: ".planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md"
      via: "$gsd-validate-phase 05 close-out"
      pattern: "## Validation Audit|nyquist_compliant: true"
---

<objective>
最后做顶层收口: 更新 foundation requirement 的 traceability 和 state，重新运行真实 milestone audit 流程，并给 Phase 05 补齐 verification + validation 收尾。

Purpose: 前三张 plan 只把旧 phase 的证据补齐，这一张负责让顶层文档和 GSD workflow 真正认账。
Output: 更新后的 `REQUIREMENTS.md`、`STATE.md`、`v1.0-MILESTONE-AUDIT.md`、`05-VERIFICATION.md`、`05-VALIDATION.md`。
</objective>

<execution_context>
@/home/acacia/.codex/get-shit-done/workflows/execute-plan.md
@/home/acacia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-foundation-artifact-backfill/05-RESEARCH.md
@.planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md
@/home/acacia/.codex/get-shit-done/workflows/audit-milestone.md
@/home/acacia/.codex/skills/gsd-audit-milestone/SKILL.md
@/home/acacia/.codex/skills/gsd-validate-phase/SKILL.md
@/home/acacia/.codex/get-shit-done/workflows/validate-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 同步顶层 traceability 和 milestone state</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/01-reference-backend-removal/01-VERIFICATION.md
    - .planning/phases/02-plugin-reconciler/02-VERIFICATION.md
    - .planning/phases/05-foundation-artifact-backfill/05-RESEARCH.md
  </read_first>
  <files>
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </files>
  <action>在旧 phase 证据都补齐以后，先更新 `.planning/REQUIREMENTS.md` 里 16 个 foundation requirement 的顶层 checklist 和 `## Traceability` 表: `REF-01`、`REF-02`、`REF-03`、`PLUG-01` 到 `PLUG-13` 都改成 `[x]`，并把 Traceability 里的 `Status` 从 `Pending` 改成 `Validated`，Phase 列继续保持 `5`。不要改动其他 requirement。然后更新 `.planning/STATE.md`: 把当前 phase 状态改成 foundation backfill 已完成的描述，在 `Verification Results` 里新增一段 Phase 05 结果，明确写 Phase 01/02 工件链已恢复、foundation orphan gap 已清空；在 `Blockers` 里删除或改写 “Phase 05 not yet planned or executed” 这一条，只保留 Phase 06/07 还没做的 blocker。不要碰与本 phase 无关的 decision 和 metrics。</action>
  <verify>
    <automated>bash -lc 'for req in REF-01 REF-02 REF-03 PLUG-01 PLUG-02 PLUG-03 PLUG-04 PLUG-05 PLUG-06 PLUG-07 PLUG-08 PLUG-09 PLUG-10 PLUG-11 PLUG-12 PLUG-13; do rg -n "\\*\\*${req}\\*\\*" .planning/REQUIREMENTS.md | rg "\\[x\\]" >/dev/null; rg -n "\\| ${req} \\| 5 \\| Validated \\|" .planning/REQUIREMENTS.md >/dev/null; done && ! rg -n "Phase 05 not yet planned or executed" .planning/STATE.md && rg -n "Phase 05|foundation|orphan" .planning/STATE.md'</automated>
  </verify>
  <acceptance_criteria>
    - 16 个 foundation requirement 在 `.planning/REQUIREMENTS.md` 顶层列表里全部变成 `[x]`。
    - 16 个 foundation requirement 在 `.planning/REQUIREMENTS.md` 的 Traceability 表里全部是 `| 5 | Validated |`。
    - `! rg -n 'Phase 05 not yet planned or executed' .planning/STATE.md` 返回成功。
    - `rg -n 'foundation|Phase 05|artifact chain' .planning/STATE.md` 有命中。
  </acceptance_criteria>
  <done>顶层 traceability 与 state 已经承认 foundation backfill 完成，后面的 audit 文件不再和 REQUIREMENTS/STATE 打架。</done>
</task>

<task type="auto">
  <name>Task 2: 运行真实 milestone audit 流程，并写 `05-VERIFICATION.md`</name>
  <read_first>
    - .planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md
    - .planning/phases/01-reference-backend-removal/01-PLAN-01-SUMMARY.md
    - .planning/phases/01-reference-backend-removal/01-PLAN-02-SUMMARY.md
    - .planning/phases/01-reference-backend-removal/01-VERIFICATION.md
    - .planning/phases/02-plugin-reconciler/02-PLAN-01-SUMMARY.md
    - .planning/phases/02-plugin-reconciler/02-PLAN-02-SUMMARY.md
    - .planning/phases/02-plugin-reconciler/02-PLAN-03-SUMMARY.md
    - .planning/phases/02-plugin-reconciler/02-VERIFICATION.md
    - .planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md
    - /home/acacia/.codex/skills/gsd-audit-milestone/SKILL.md
    - /home/acacia/.codex/get-shit-done/workflows/audit-milestone.md
  </read_first>
  <files>
    .planning/v1.0-MILESTONE-AUDIT.md
    .planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md
  </files>
  <action>不要手改 `.planning/v1.0-MILESTONE-AUDIT.md`。先执行真实的 milestone audit 流程: `$gsd-audit-milestone v1.0`，让 workflow 自己重新读取 `REQUIREMENTS.md`、Phase 01/02 的 `VERIFICATION.md`、以及所有新补的 summary frontmatter，然后生成新的 `.planning/v1.0-MILESTONE-AUDIT.md`。只有 audit workflow 输出的新报告可以当证据源。跑完以后检查 16 个 requirement: `REF-01..03` 和 `PLUG-01..13` 必须全部出现在 audit 报告里，且 `status` 不能再是 `orphaned`、`partial`、`unsatisfied`。如果 audit 仍报这 16 条里的任意一条有 gap，就停下来，不要用手写 `05-VERIFICATION.md` 把问题糊过去。audit 通过后再新建 `05-VERIFICATION.md`，结构参考 `04-VERIFICATION.md`，frontmatter 至少写 `phase: 05-foundation-artifact-backfill`、`verified: <UTC 时间>`、`status: passed`、`score: 16/16 requirements verified`、`gaps: []`。正文要写清三件事: old basename summary matching 已恢复，Phase 01/02 verification + validation 已补齐，foundation requirement 在重新运行的 audit 里已经全部收口。`## Requirements Coverage` 表里把 16 个 requirement 全列出来，并在 `Evidence` 一列指向对应 phase 的 summary / verification / audit 文件。</action>
  <verify>
    <automated>bash -lc 'audit=.planning/v1.0-MILESTONE-AUDIT.md; ver=.planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md; rg -n "^status: passed" "$ver" >/dev/null; for req in REF-01 REF-02 REF-03 PLUG-01 PLUG-02 PLUG-03 PLUG-04 PLUG-05 PLUG-06 PLUG-07 PLUG-08 PLUG-09 PLUG-10 PLUG-11 PLUG-12 PLUG-13; do rg -n "$req" "$audit" >/dev/null; rg -n "$req" "$ver" >/dev/null; ! rg -n "$req.*(orphaned|partial|unsatisfied)" "$audit" >/dev/null; done'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.0-MILESTONE-AUDIT.md` 是 `$gsd-audit-milestone v1.0` 重新生成的输出，不是手工编辑拼出来的结果。
    - 16 个 foundation requirement 全部出现在 `.planning/v1.0-MILESTONE-AUDIT.md`，且没有任何一条是 `orphaned`、`partial`、`unsatisfied`。
    - `.planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md` 存在，且 `rg -n '^status: passed'` 有命中。
    - 16 个 foundation requirement 全部出现在 `05-VERIFICATION.md` 的 `## Requirements Coverage` 表里。
    - `rg -n 'completed_by_plans|verification_status|status' .planning/v1.0-MILESTONE-AUDIT.md` 有命中。
  </acceptance_criteria>
  <done>顶层 audit 文件与 Phase 05 自己的 verification 都已经写好，foundation orphan gap 真正关掉，而不是只在下层 phase 目录里看起来像补完了。</done>
</task>

<task type="auto">
  <name>Task 3: 通过 `$gsd-validate-phase 05` 收掉 `05-VALIDATION.md`</name>
  <read_first>
    - .planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md
    - .planning/phases/05-foundation-artifact-backfill/05-VERIFICATION.md
    - /home/acacia/.codex/skills/gsd-validate-phase/SKILL.md
    - /home/acacia/.codex/get-shit-done/workflows/validate-phase.md
  </read_first>
  <files>
    .planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md
  </files>
  <action>在 Task 2 通过以后，显式执行 `$gsd-validate-phase 05`，按真实 validate workflow 审计 Phase 05 现有的 `PLAN.md`、`SUMMARY.md`、`VERIFICATION.md` 和 `05-VALIDATION.md`。如果 workflow 在 execute mode 里给出 “Fix all gaps / Skip / Cancel” 选择，默认走 “Fix all gaps”。执行结束后把 `05-VALIDATION.md` 更新成真正的收口结果: 保留 `nyquist_compliant: true`，追加这次 audit 的 `## Validation Audit` 记录，并把当前 frontmatter / sign-off 从 draft + pending 改成已处理状态。不要把 `05-VALIDATION.md` 留在现在这种 `status: draft`、`Approval: pending` 的半成品状态。</action>
  <verify>
    <automated>bash -lc 'val=.planning/phases/05-foundation-artifact-backfill/05-VALIDATION.md; rg -n "^nyquist_compliant: true" "$val" >/dev/null && rg -n "^status: " "$val" >/dev/null && rg -n "## Validation Audit" "$val" >/dev/null && ! rg -n "^status: draft$|Approval: pending" "$val" >/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - `05-VALIDATION.md` 经过 `$gsd-validate-phase 05` 真实更新。
    - `05-VALIDATION.md` 里有这次收口追加的 `## Validation Audit` 记录。
    - `05-VALIDATION.md` 不再保留 `status: draft` 和 `Approval: pending`。
  </acceptance_criteria>
  <done>Phase 05 的 Nyquist validation 也被真实收口，后续不需要再猜这张 validation 文件到底算草稿还是完成态。</done>
</task>

</tasks>

<verification>
先确认 `.planning/REQUIREMENTS.md` 的 16 个 foundation requirement 都已经是 `[x]` + `Validated`。再确认 `$gsd-audit-milestone v1.0` 重新生成的 `.planning/v1.0-MILESTONE-AUDIT.md` 里，这 16 条 requirement 全部存在且都不是 `orphaned`、`partial`、`unsatisfied`。最后确认 `05-VERIFICATION.md` 和经过 `$gsd-validate-phase 05` 更新的 `05-VALIDATION.md` 都存在，并且 `05-VALIDATION.md` 带有新的 `## Validation Audit` 记录。
</verification>

<success_criteria>
1. `.planning/REQUIREMENTS.md` 里的 REF-01..03 和 PLUG-01..13 全部改成 `[x]`，Traceability 全部改成 `Validated`。
2. 重新运行 `$gsd-audit-milestone v1.0` 后，`.planning/v1.0-MILESTONE-AUDIT.md` 不再把 16 个 foundation requirement 中的任何一条标成 `orphaned`、`partial`、`unsatisfied`。
3. `05-VERIFICATION.md` 清楚记录本 phase 关闭了哪些 foundation artifact gap，并覆盖全部 16 个 requirement。
4. `05-VALIDATION.md` 经过 `$gsd-validate-phase 05` 真正收口，不再停留在 draft。
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-artifact-backfill/05-04-SUMMARY.md`
</output>
