---
phase: 07-render-readability-workspace-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime_config/prompts/aca.md
  - docs/12-computer.md
autonomous: true
requirements:
  - MSG-08

must_haves:
  truths:
    - "system prompt 中明确告诉模型: 工作区全部在 /workspace"
    - "system prompt 中明确告诉模型: 发送 QQ 本地文件时只用 /workspace 里的内容并以相对路径引用"
    - "render 产物不属于 Work World, 不受 /workspace 发送规则约束"
    - "文档准确反映了 /workspace 语义和 render artifact 的边界"
  artifacts:
    - path: "runtime_config/prompts/aca.md"
      provides: "更新后的 system prompt, 包含 workspace 规则"
      contains: "/workspace"
    - path: "docs/12-computer.md"
      provides: "更新后的 computer 文档, 反映 render 边界"
      contains: "render_artifacts"
  key_links:
    - from: "runtime_config/prompts/aca.md"
      to: "src/acabot/runtime/context_assembly/assembler.py"
      via: "prompt loader 加载并注入 system prompt"
      pattern: "工作区"
---

<objective>
把 workspace 工作语义写进 system prompt, 让模型明确知道: 工作区在 /workspace; 发送 QQ 本地文件时只从 /workspace 按相对路径引用; render 是系统自动流程, 不受这条规则约束。同时更新 12-computer.md 文档以反映真实边界。

Purpose: Per D-10/D-11/D-12/D-13, 收稳"模型工作语义"。Per D-14/D-15/D-16, render 不受 workspace 发送规则约束。
Output: 更新后的 system prompt 文件和 computer 文档。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-render-readability-workspace-boundary/07-CONTEXT.md

<interfaces>
<!-- workspace 语义在代码中的实际实现 -->

From src/acabot/runtime/computer/world.py:
```python
class _WorldViewImpl(WorldView):
    # /workspace -> workspace_root_host_path (由 WorkspaceManager 分配)
    # /skills -> skills_root_host_path
    # /self -> self_root_host_path
    def resolve(self, world_path: str) -> ResolvedWorldPath:
        # 把 world path 解析成宿主机路径
        # root_kind 只接受 "workspace", "skills", "self"
```

From src/acabot/runtime/computer/workspace.py:
```python
class WorkspaceManager:
    def workspace_dir_for_thread(self, thread_id: str) -> Path:
        # 返回 root_dir / "threads" / safe(thread_id) / "workspace"
```

From src/acabot/runtime/render/artifacts.py:
```python
def render_artifacts(...) -> RenderArtifacts:
    # 路径: runtime_data/render_artifacts/{conversation_id}/{run_id}/
    # 不在 /workspace/attachments 下
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 system prompt 中添加 workspace 工作规则</name>
  <files>runtime_config/prompts/aca.md</files>
  <read_first>
    runtime_config/prompts/aca.md
    src/acabot/runtime/computer/world.py
    src/acabot/runtime/computer/contracts.py
  </read_first>
  <action>
    在 `runtime_config/prompts/aca.md` 中添加以下工作区规则段落。位置放在已有的工作说明之后、工具说明之前:

    ```markdown
    ## 工作区

    - 你的所有工作文件都在 `/workspace` 目录下。使用 `computer_read`、`computer_write`、`computer_bash` 操作文件时, 路径以 `/workspace/` 开头。
    - 如需发送 `/workspace` 中的文件到 QQ, 使用相对路径引用。例如: `/workspace/report.pdf` 在发送时引用为 `report.pdf`。
    - 不要使用系统临时目录 (如 `/tmp/`) 存放工作产物。
    - `render_markdown_to_image` 是系统自动渲染流程。你只需要提供待渲染的内容, 渲染产物的存储和发送由系统自动完成, 不受上述文件路径规则约束。
    ```

    注意:
    - 措辞要非常明确具体 (per RESEARCH Pitfall 5)
    - render 段落呼应 D-14/D-15/D-16 的设计决策
    - 这个文件路径 `runtime_config/prompts/aca.md` 是项目实际的 system prompt 文件。如果项目中使用的是不同的 prompt 文件名 (例如通过 session config 的 prompt_ref 字段指定), 请在执行时先用 `grep -r "prompt_ref" runtime_config/sessions/` 确认实际引用的 prompt 文件, 然后修改正确的那个文件
    - 如果 `runtime_config/prompts/aca.md` 不存在, 先检查 `runtime_config/prompts/` 下有哪些 `.md` 文件, 选择被主 session agent 引用的那个
  </action>
  <verify>
    <automated>grep -c "/workspace" runtime_config/prompts/aca.md || grep -c "/workspace" runtime_config/prompts/*.md</automated>
  </verify>
  <done>
    - system prompt 包含 "工作区" 段落
    - 明确说明工作文件在 /workspace
    - 明确说明发送 QQ 文件时使用相对路径
    - 明确说明 render 不受 workspace 规则约束
    - 不要求 host shell 虚拟化 (per D-10)
  </done>
  <acceptance_criteria>
    - `grep -c "工作区" runtime_config/prompts/aca.md` 返回大于 0 的数字 (或在正确的 prompt 文件中)
    - `grep -c "/workspace" runtime_config/prompts/aca.md` 返回大于 2 的数字
    - `grep -c "render_markdown_to_image" runtime_config/prompts/aca.md` 返回大于 0 的数字
    - `grep -c "相对路径" runtime_config/prompts/aca.md` 返回大于 0 的数字
    - `grep -c "/tmp/" runtime_config/prompts/aca.md` 返回大于 0 的数字 (提及不要用 tmp)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 docs/12-computer.md 反映 render artifact 边界</name>
  <files>docs/12-computer.md</files>
  <read_first>
    docs/12-computer.md
    src/acabot/runtime/render/artifacts.py
  </read_first>
  <action>
    在 docs/12-computer.md 中找到关于 render artifact 路径的章节。确保文档准确描述以下事实:

    1. render 产物存储在 `runtime_data/render_artifacts/{conversation_id}/{run_id}/`, 不在 `/workspace/attachments/` 下
    2. render 是工具驱动的 runtime 内部流程, 模型只提供待渲染内容
    3. `/workspace` 是模型侧正式工作语义, 真实宿主机路径属于 runtime 内部实现
    4. host backend 下 shell 看到真实宿主机路径, docker backend 下 shell 看到 `/workspace`

    如果文档中已有 render 相关描述但与上述不一致, 更新为一致。如果文档中完全没有 render artifact 边界说明, 在 Work World 章节之后添加 "Render Artifact 边界" 小节。

    具体添加/更新的内容:
    ```markdown
    ### Render Artifact 边界

    `render_markdown_to_image` 的产物存储在 `runtime_data/render_artifacts/` 下, 不落入 Work World 的 `/workspace/attachments/`。这是设计决策, 不是实现疏漏:

    - render 是工具驱动的系统自动流程, 模型只提供 markdown 内容
    - render 产物的存储路径和发送由 Outbox 自动处理, 不需要模型手动选择路径
    - 因此 render 产物不受 "模型只从 /workspace 发送本地文件" 这条规则的约束
    ```
  </action>
  <verify>
    <automated>grep -c "render_artifacts" docs/12-computer.md</automated>
  </verify>
  <done>
    - docs/12-computer.md 包含 render artifact 边界说明
    - 文档正确说明 render 产物不在 /workspace/attachments 下
    - 文档正确说明 /workspace 是模型侧语义
  </done>
  <acceptance_criteria>
    - `grep -c "render_artifacts" docs/12-computer.md` 返回大于 0
    - `grep -c "Render Artifact" docs/12-computer.md` 返回大于 0
    - `grep -c "工具驱动的系统自动流程" docs/12-computer.md` 返回大于 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. system prompt 包含完整的 workspace 工作规则
2. docs/12-computer.md 包含 render artifact 边界说明
3. 所有描述与 D-10..D-16 决策一致
</verification>

<success_criteria>
- 模型通过 system prompt 明确知道工作区在 /workspace (per D-11)
- 模型通过 system prompt 知道发送 QQ 文件用相对路径 (per D-12)
- render 不受 workspace 发送规则约束 (per D-14/D-15/D-16)
- host shell 真实路径问题不在 scope 内 (per D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/07-render-readability-workspace-boundary/07-02-SUMMARY.md`
</output>
