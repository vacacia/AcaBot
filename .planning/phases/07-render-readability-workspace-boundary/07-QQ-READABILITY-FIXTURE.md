# Render 可读性验收素材

> 用于真实 QQ 客户端的 `message.send.render` 可读性验收。单份素材固定覆盖 Phase 7 要求的 8 类内容。

# 一级标题

这是一段普通正文，用来确认 QQ 客户端里的常规中文文本是否清晰可读。

- 列表项一
- 列表项二
- 列表项三

行内公式：$E = mc^2$

$$
\int_0^1 x^2 dx = \frac{1}{3}
$$

```python
for i in range(3):
    print(i)
```

> 这是一段引用块，用来确认引用文字边界和底色是否仍然可读。

| 列 1 | 列 2 | 列 3 |
| --- | --- | --- |
| A | B | C |
| 1 | 2 | 3 |
