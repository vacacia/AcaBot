---
phase: 07-render-readability-workspace-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/acabot/runtime/render/playwright_backend.py
  - src/acabot/runtime/bootstrap/__init__.py
  - config.example.yaml
  - tests/runtime/test_render_service.py
autonomous: true
requirements:
  - MSG-08

must_haves:
  truths:
    - "Playwright render 产出的截图物理像素是 CSS 像素的 2 倍 (device_scale_factor=2)"
    - "device_scale_factor 和 viewport_width 通过 config.yaml 配置, 不在代码中写死"
    - "修改 render 配置后下一个 render 自动生效, 不需要重启 browser"
    - "每次 render 后 context 和 page 都被正确关闭, 无内存泄漏"
  artifacts:
    - path: "src/acabot/runtime/render/playwright_backend.py"
      provides: "HiDPI Playwright render backend"
      contains: "device_scale_factor"
    - path: "src/acabot/runtime/bootstrap/__init__.py"
      provides: "render 配置注入到 PlaywrightRenderBackend"
      contains: "render_config"
    - path: "config.example.yaml"
      provides: "render 配置示例"
      contains: "render:"
    - path: "tests/runtime/test_render_service.py"
      provides: "HiDPI + context 清理 + 配置传参测试"
      contains: "test_hidpi"
  key_links:
    - from: "src/acabot/runtime/bootstrap/__init__.py"
      to: "src/acabot/runtime/render/playwright_backend.py"
      via: "PlaywrightRenderBackend 构造函数传参"
      pattern: "PlaywrightRenderBackend\\("
    - from: "config.example.yaml"
      to: "bootstrap"
      via: "config.get('runtime', {}).get('render', {})"
      pattern: "device_scale_factor"
---

<objective>
把 Playwright render backend 从 1x 截图升级到 HiDPI 截图, 并把 device_scale_factor 和 viewport_width 外置为 config.yaml 里的运行时可调配置。

Purpose: Per D-01/D-02, 当前问题是"像素不够清晰"。通过 device_scale_factor=2 让 Playwright 在截图时生成真正的 2x 物理像素, 让 render 产物在 QQ 客户端压缩后仍可读。Per D-03/D-04/D-05, 这两个参数必须外置为全局默认值级别的配置。
Output: 改造后的 PlaywrightRenderBackend, config.yaml render 配置段, 更新的测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-render-readability-workspace-boundary/07-CONTEXT.md
@.planning/phases/07-render-readability-workspace-boundary/07-RESEARCH.md

<interfaces>
<!-- executor 必须理解的关键接口 -->

From src/acabot/runtime/render/playwright_backend.py:
```python
class PlaywrightRenderBackend:
    name = "playwright"
    def __init__(self, *, start_playwright: StartPlaywright | None = None) -> None:
        # 当前构造函数只接受 start_playwright, 需要新增 device_scale_factor 和 viewport_width
    async def render_markdown_to_image(self, request: RenderRequest) -> RenderResult:
        # 当前使用 browser.new_page() + set_viewport_size(960, 540), 需改为 new_context + new_page
    async def close(self) -> None:
        # 关闭 browser 和 playwright
```

From src/acabot/runtime/render/protocol.py:
```python
@dataclass(slots=True)
class RenderRequest:
    conversation_id: str
    run_id: str
    source_markdown: str
    artifacts: RenderArtifacts
    metadata: dict[str, Any] = field(default_factory=dict)

class RenderBackend(Protocol):
    name: str
    async def render_markdown_to_image(self, request: RenderRequest) -> RenderResult: ...
    async def close(self) -> None: ...
```

From src/acabot/runtime/bootstrap/__init__.py (line ~282-290):
```python
runtime_render_service = RenderService(
    runtime_root=resolve_runtime_path(config, ""),
)
runtime_playwright_render_backend = PlaywrightRenderBackend()
runtime_render_service.register_backend(
    runtime_playwright_render_backend.name,
    runtime_playwright_render_backend,
    is_default=True,
)
```

From tests/runtime/test_render_service.py:
```python
class FakePage:
    # 只有 set_viewport_size / set_content / screenshot / close
class FakeBrowser:
    # 只有 new_page / close -- 需要 new_context 支持
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlaywrightRenderBackend HiDPI 改造 + context 泄漏防护</name>
  <files>src/acabot/runtime/render/playwright_backend.py</files>
  <read_first>
    src/acabot/runtime/render/playwright_backend.py
    src/acabot/runtime/render/protocol.py
  </read_first>
  <action>
    1. 在 `PlaywrightRenderBackend.__init__` 中新增两个参数:
       - `device_scale_factor: float = 2.0` — 存为 `self._device_scale_factor`
       - `viewport_width: int = 960` — 存为 `self._viewport_width`
       保留原有的 `start_playwright` 参数不变。

    2. 重写 `render_markdown_to_image` 方法核心逻辑:
       ```python
       async def render_markdown_to_image(self, request: RenderRequest) -> RenderResult:
           document_html = self._build_document(request.source_markdown)
           request.artifacts.html_path.write_text(document_html, encoding="utf-8")
           page = None
           context = None
           try:
               browser = await self._ensure_browser()
               context = await browser.new_context(
                   viewport={"width": self._viewport_width, "height": 720},
                   device_scale_factor=self._device_scale_factor,
               )
               page = await context.new_page()
               await page.set_content(document_html, wait_until="load")
               await page.screenshot(
                   path=str(request.artifacts.image_path),
                   full_page=True,
                   type="png",
               )
               return RenderResult.ok(
                   backend_name=self.name,
                   artifact_path=request.artifacts.image_path,
                   html=document_html,
                   metadata={
                       "html_path": str(request.artifacts.html_path),
                       "device_scale_factor": self._device_scale_factor,
                       "viewport_width": self._viewport_width,
                   },
               )
           except Exception as exc:
               return RenderResult.error_result(
                   backend_name=self.name,
                   artifact_path=request.artifacts.image_path,
                   html=document_html,
                   error=str(exc),
                   metadata={"html_path": str(request.artifacts.html_path)},
               )
           finally:
               if page is not None:
                   await page.close()
               if context is not None:
                   await context.close()
       ```

    3. 同时修改 HTML_TEMPLATE 中 `.render-shell` 的固定宽度:
       将 `width: 960px;` 改为 `max-width: 100%;`。
       不再硬编码 CSS 宽度, 让 viewport 宽度决定布局。这避免 viewport_width 改变后布局断裂 (per RESEARCH Pitfall 4)。

    注意:
    - `device_scale_factor` 只在 `browser.new_context()` 级别生效, 不在 `page` 级别
    - 每次 render 创建新 context, 渲染完关闭, 所以改配置后自动生效
    - `finally` 块必须同时关闭 page 和 context, 防止内存泄漏 (per RESEARCH Pitfall 2)
  </action>
  <verify>
    <automated>python -m pytest tests/runtime/test_render_service.py -x --ignore=tests/runtime/backend/test_pi_adapter.py -q</automated>
  </verify>
  <done>
    - PlaywrightRenderBackend 接受 device_scale_factor 和 viewport_width 参数
    - render_markdown_to_image 使用 browser.new_context() + context.new_page()
    - finally 块同时关闭 page 和 context
    - HTML_TEMPLATE 中 .render-shell 不再有硬编码 960px 宽度
    - 现有测试仍通过 (需要更新 FakeBrowser 以支持 new_context)
  </done>
  <acceptance_criteria>
    - `grep -n "new_context" src/acabot/runtime/render/playwright_backend.py` 返回包含 `browser.new_context` 的行
    - `grep -n "device_scale_factor" src/acabot/runtime/render/playwright_backend.py` 返回至少 3 行 (构造函数 + new_context 调用 + metadata)
    - `grep -n "context.close" src/acabot/runtime/render/playwright_backend.py` 返回 finally 块中的关闭调用
    - HTML_TEMPLATE 中不再包含 `width: 960px`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新测试桩 + 新增 HiDPI/context/配置测试</name>
  <files>tests/runtime/test_render_service.py</files>
  <read_first>
    tests/runtime/test_render_service.py
    src/acabot/runtime/render/playwright_backend.py
  </read_first>
  <action>
    1. 更新 FakeBrowser 类, 新增 new_context 方法:
       ```python
       class FakeBrowser:
           def __init__(self) -> None:
               self.new_page_calls = 0
               self.new_context_calls = 0
               self.pages: list[FakePage] = []
               self.contexts: list[FakeContext] = []
               self.closed = 0

           async def new_page(self) -> FakePage:
               self.new_page_calls += 1
               page = FakePage()
               self.pages.append(page)
               return page

           async def new_context(self, **kwargs) -> FakeContext:
               self.new_context_calls += 1
               ctx = FakeContext(kwargs)
               self.contexts.append(ctx)
               return ctx

           async def close(self) -> None:
               self.closed += 1
       ```

    2. 新增 FakeContext 类:
       ```python
       class FakeContext:
           def __init__(self, options: dict) -> None:
               self.options = dict(options)
               self.page: FakePage | None = None
               self.closed = 0

           async def new_page(self) -> FakePage:
               page = FakePage()
               self.page = page
               return page

           async def close(self) -> None:
               self.closed += 1
       ```

    3. 更新现有 `test_playwright_backend_reuses_single_browser` 测试:
       - 将断言 `fake_browser.new_page_calls == 2` 改为 `fake_browser.new_context_calls == 2`
       - 验证每次 render 创建一个新 context
       - 验证所有 context 都被关闭: `assert all(ctx.closed == 1 for ctx in fake_browser.contexts)`

    4. 新增测试 `test_render_uses_hidpi_context`:
       - 构造 `PlaywrightRenderBackend(device_scale_factor=3.0, viewport_width=800, start_playwright=...)`
       - 执行一次 render
       - 断言 `fake_browser.contexts[0].options["device_scale_factor"] == 3.0`
       - 断言 `fake_browser.contexts[0].options["viewport"]["width"] == 800`
       - 断言 `result.metadata["device_scale_factor"] == 3.0`
       - 断言 `result.metadata["viewport_width"] == 800`

    5. 新增测试 `test_render_context_closed_after_success`:
       - 执行一次 render
       - 断言 context.closed == 1
       - 断言 page.closed == 1

    6. 新增测试 `test_render_context_closed_after_error`:
       - 构造一个在 screenshot 时抛异常的 FakePage
       - 执行 render
       - 断言 result.status == "error"
       - 断言 context 仍然被关闭 (context.closed == 1)
  </action>
  <verify>
    <automated>python -m pytest tests/runtime/test_render_service.py -x --ignore=tests/runtime/backend/test_pi_adapter.py -q</automated>
  </verify>
  <done>
    - FakeBrowser 支持 new_context() 方法
    - FakeContext 类存在, 有 new_page() 和 close()
    - 现有测试全部通过 (browser reuse, pipeline, artifacts)
    - 新增 3 个测试: HiDPI 参数传递, context 成功关闭, context 异常关闭
  </done>
  <acceptance_criteria>
    - `grep -n "class FakeContext" tests/runtime/test_render_service.py` 返回 FakeContext 定义
    - `grep -n "test_render_uses_hidpi_context" tests/runtime/test_render_service.py` 返回测试函数
    - `grep -n "test_render_context_closed_after_success" tests/runtime/test_render_service.py` 返回测试函数
    - `grep -n "test_render_context_closed_after_error" tests/runtime/test_render_service.py` 返回测试函数
    - `python -m pytest tests/runtime/test_render_service.py -x --ignore=tests/runtime/backend/test_pi_adapter.py -q` 退出码 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: config.yaml render 配置段 + bootstrap 注入</name>
  <files>config.example.yaml, src/acabot/runtime/bootstrap/__init__.py</files>
  <read_first>
    config.example.yaml
    src/acabot/runtime/bootstrap/__init__.py
    src/acabot/runtime/render/playwright_backend.py
  </read_first>
  <action>
    1. 在 config.example.yaml 的 `runtime:` 段中新增 render 配置:
       ```yaml
         # --- Render 配置 ---
         render:
           device_scale_factor: 2.0
           viewport_width: 960
       ```
       放在 `webui:` 段之前。

    2. 修改 src/acabot/runtime/bootstrap/__init__.py 中的 render backend 构造:
       将:
       ```python
       runtime_playwright_render_backend = PlaywrightRenderBackend()
       ```
       改为:
       ```python
       render_config = dict(runtime_conf.get("render", {}) or {})
       runtime_playwright_render_backend = PlaywrightRenderBackend(
           device_scale_factor=float(render_config.get("device_scale_factor", 2.0)),
           viewport_width=int(render_config.get("viewport_width", 960)),
       )
       ```

    注意: `runtime_conf` 变量在 bootstrap 中已经定义 (line ~133: `runtime_conf = config.get("runtime", {})`)，直接使用。
  </action>
  <verify>
    <automated>python -m pytest tests/runtime/test_render_service.py -x --ignore=tests/runtime/backend/test_pi_adapter.py -q</automated>
  </verify>
  <done>
    - config.example.yaml 包含 runtime.render 段, 含 device_scale_factor 和 viewport_width
    - bootstrap 从 config 读取 render 配置并传给 PlaywrightRenderBackend
    - 不配置 render 段时回退到默认值 device_scale_factor=2.0, viewport_width=960
  </done>
  <acceptance_criteria>
    - `grep -n "render:" config.example.yaml` 返回 render 配置段
    - `grep -n "device_scale_factor" config.example.yaml` 返回配置项
    - `grep -n "render_config" src/acabot/runtime/bootstrap/__init__.py` 返回 bootstrap 中的配置读取
    - `grep -n "PlaywrightRenderBackend(" src/acabot/runtime/bootstrap/__init__.py` 返回带参数的构造调用
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 全量 render 测试通过: `python -m pytest tests/runtime/test_render_service.py -x --ignore=tests/runtime/backend/test_pi_adapter.py`
2. PlaywrightRenderBackend 使用 `browser.new_context(device_scale_factor=...)` 而非 `browser.new_page()`
3. config.example.yaml 包含 render 配置段
4. bootstrap 正确读取配置并传参
5. HTML 模板中不再有硬编码 960px 宽度
</verification>

<success_criteria>
- device_scale_factor 和 viewport_width 通过 config.yaml 可配置 (per D-03, D-04, D-05)
- 默认 device_scale_factor=2.0, viewport_width=960 (per D-06, planner 决定初始值)
- 每次 render 自动关闭 context, 无内存泄漏
- 所有现有测试 + 3 个新增测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-render-readability-workspace-boundary/07-01-SUMMARY.md`
</output>
